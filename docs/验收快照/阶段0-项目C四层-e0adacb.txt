== 项目C 接口(运行侧 / 评测侧职责分离)==
  FAIL 暴露 retrieve()
    → 缺少函数 → 无法把四层归因做成可测试的行为
  FAIL 暴露 generate()
    → 缺少函数 → 无法把四层归因做成可测试的行为
  FAIL 暴露 validate()
    → 缺少函数 → 无法把四层归因做成可测试的行为
  FAIL 暴露 diagnose()
    → 缺少函数 → 无法把四层归因做成可测试的行为
  FAIL 暴露 coverage_oracle()
    → 缺少函数 → 无法把四层归因做成可测试的行为
  PASS 暴露 evaluate()
  FAIL 存在唯一固定配置 CONFIG
    → getattr(M,'CONFIG')=None

== G-1 「库里到底有没有」不能由运行侧的关键词表决定 ==
  FAIL G-1a 库里存在该知识时必须答出(引用 d5)
    → 无 diagnose()
  FAIL G-1b 运行侧不得出现「库里没有」式断言(它无从知道)
    → 无 diagnose()

== G-2 库外主题的层级归因(评测侧独立 oracle)==
  FAIL G-2 评测侧具备独立 oracle 与逐条层级归因
    → 缺 coverage_oracle()/evaluate()

== G-3 第③层(证据组织不完整)必须被真实检出 ==
  FAIL G-3 源码中存在第③层的产出路径
    → 源码里没有任何 layer=3 → 第③层只是打印对照,从未被系统诊断出来

== G-4 第④层校验必须检查回答内容,而不是看一个开关 ==

== G-5 0 分文档不得作为「候选证据」展示 ==
  FAIL G-5 0 分文档不出现在候选里(必须显式丢弃或标注)
    → 候选含 0 分文档 ['d1', 'd2']

== G-6 失败层级由数据状态与固定配置决定,不靠逐题手调参数 ==
  PASS G-6a 评测集不得携带逐题阈值

结果: 2 通过, 11 失败
失败项(基线缺陷证据):
  - 暴露 retrieve()
  - 暴露 generate()
  - 暴露 validate()
  - 暴露 diagnose()
  - 暴露 coverage_oracle()
  - 存在唯一固定配置 CONFIG
  - G-1a 库里存在该知识时必须答出(引用 d5)
  - G-1b 运行侧不得出现「库里没有」式断言(它无从知道)
  - G-2 评测侧具备独立 oracle 与逐条层级归因
  - G-3 源码中存在第③层的产出路径
  - G-5 0 分文档不出现在候选里(必须显式丢弃或标注)
