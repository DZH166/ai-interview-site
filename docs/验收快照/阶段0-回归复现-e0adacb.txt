
== 问题 B:恢复规则不确定 / 字段顺序依赖(projectDrafts 字段级合并)==
    [观测] 本地字段插入顺序 = ["runOutput","updatedAt","debug","todo"]
    [观测] 恢复后的草稿 = {"runOutput":"备份新输出","updatedAt":9000,"debug":"本地旧定位","todo":"本地旧待办"}
  PASS B-1 更新的备份:第一个字段被恢复
  FAIL B-1 更新的备份:第二个字段也被恢复 
    → debug=本地旧定位(仍为本地旧值 → 静默丢弃)
  FAIL B-1 更新的备份:第三个字段也被恢复 
    → todo=本地旧待办(仍为本地旧值 → 静默丢弃)
  FAIL B-1 未采用内容时不得抬高 updatedAt 
    → updatedAt=9000 但 debug/todo 未被采用 → 混合状态且旧备份此后永远无法恢复
    [观测] 清空后恢复旧备份 = {"runOutput":"用户早就删掉的内容","updatedAt":2000,"debug":"保留"}
  FAIL B-2 本地更新的明确清空不被旧备份复活 
    → runOutput="用户早就删掉的内容"(旧备份 updatedAt=1000 < 本地 2000,却仍然复活)

== 问题 C:无 runId 的历史项目运行记录被静默丢弃 ==
    [观测] 恢复的项目运行记录 = []
    [观测] importFull 返回统计 = {"qMerged":0,"roundsAdded":0,"notesUpdated":0,"questionsAdded":0,"docsAdded":0}
  FAIL C 无 runId 的旧运行记录必须被保留(可迁移补齐 id)而不是静默丢弃 
    → 恢复后条数=0 → 静默丢失,且界面显示「已提交 0 次」
  PASS C 迁移后重复导入仍幂等

== 问题 E-4:「最新一次尝试」按数组顺序而非时间判定 ==
    [观测] 待消化专项队列 = [{"drillId":"drill-1pred01","selfRating":"unsolved","review":"旧的误解原因","q":"第一题","updatedAt":100}]
  FAIL E-4 最新一次自评是「已解决」时不得进入待消化队列 
    → 队列条数=1 → 取到了数组末位(旧记录),把已解决的问题又拎回来
  PASS E-4 最新一次确实未解决时必须进入队列

== 问题 F:个人项目运行/排查记录不可检索 ==
    [观测] 检索项目运行记录命中数 = 0
    [观测] 检索项目草稿命中数 = 0
  FAIL F 已保存的项目运行记录可被全文检索到 
    → 命中 0 条 → 用户保存的证据搜不到
  FAIL F 项目草稿可被全文检索到 
    → 命中 0 条 → 未提交的草稿搜不到

== 问题 H-1:测试中的恒真断言 ==
    [观测] 恒真断言所在行 = [524]
  FAIL H-1 断言不得恒真(形如 `... || true`) 
    → 1 处恒真断言:L524 ok('重复恢复幂等', (Store.data.ui.projectRuns || {})['proj-a-model
  PASS H-1 真实语义:重复完整恢复后运行记录不翻倍

== 问题 H-2:清空记录后检索索引未失效(搜得到已清空的数据)==
    [观测] 清空前命中数 = 1
  PASS H-2 前置:清空前确实可检索到
    [观测] 清空后(未重建索引)命中数 = 1
  FAIL H-2 清空全部记录后检索不得再返回已清空的数据 
    → 仍命中 1 条 → 清空后搜索还能搜出已删笔记/尝试,点进去是空的

结果: 5 通过, 10 失败
失败项(基线缺陷证据):
  - B-1 更新的备份:第二个字段也被恢复
  - B-1 更新的备份:第三个字段也被恢复
  - B-1 未采用内容时不得抬高 updatedAt
  - B-2 本地更新的明确清空不被旧备份复活
  - C 无 runId 的旧运行记录必须被保留(可迁移补齐 id)而不是静默丢弃
  - E-4 最新一次自评是「已解决」时不得进入待消化队列
  - F 已保存的项目运行记录可被全文检索到
  - F 项目草稿可被全文检索到
  - H-1 断言不得恒真(形如 `... || true`)
  - H-2 清空全部记录后检索不得再返回已清空的数据
