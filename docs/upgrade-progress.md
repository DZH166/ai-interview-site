# 自主升级进度(任务书基线 bab5570)

> 本文件是长任务恢复点。每完成一个批次更新;「完成」=有验证证据,不是"代码已写"。

## 阶段0:工作台 ✅

- 基线:HEAD `bab5570`,工作树干净后按本轮任务推进(改动未提交,等阶段验收)。
- 验证环境:Windows 10 / Python 3.13.9 / pydantic 2.10.4 / openai 2.34.0(仅异常类)/ Node 24;浏览器为隔离源 127.0.0.1:8766;用户真实数据在 8765 未动。
- 回归 fixture:behavior-tests(81 项)已覆盖旧数字进度、对象进度、取消状态、完整备份、真实来源枚举(7 种)、坏导入、隔离容量失败、草稿恢复;path_exercise_test 覆盖展示代码轨迹;lp003_mock_test 覆盖五类失败分支。
- 命令:`node tests/behavior-tests.js`、`python tests/lp003_mock_test.py`、`python tests/path_exercise_test.py`、`python tools/validate_bank.py`、`python tools/dedup_check.py`、`python tools/build.py`。

## 阶段1:修净四组残留

### 1.1 阶段状态统一 ✅
- PathView 增 `stageStatus()`:按最新事件(done vs cancelled)判定,旧 number 迁移,空/null→none;显示用有效时间戳(fmtTime 不再 NaN);取消改写 `cancelled` 事件、**不再删除条目**(done 留痕,旧备份不会复活完成态);页面显示「已取消确认」状态与「留痕」按钮文案。
- 验证:behavior-tests 10 项断言(含报告原用例:{done 早,cancelled 晚}→未完成;旧备份导入不复活;更新的备份完成→done)。
- 顺带修正:mergePathProgress 语义确认为"比较最新事件时间戳"(原实现即按 max 事件时间比较,行为正确;测试 fixture 原先构造错误)。

### 1.2 阶段6 工具失败协议 ✅
- fake model 收到 `{'error':...}` 结果 → 输出「查询失败(原因)」,不再拼「适合出行」成功建议;成功路径不受影响。
- path_exercise_test 加强(16→19 项):未知工具/工具异常都断言"失败说明出现 + 成功建议不出现 + 调用次数";成功路径断言失败字样不出现(协议不误伤)。

### 1.3 阶段3 三种收尾对照 ✅
- 实测轨迹(Python 3.13.9,tests/_r13_gather_traces.py):对照1 gather+捕获:a/c 完成、未被取消;对照3 TaskGroup:c 被取消+ExceptionGroup;对照2 异常逃出 main:运行器收尾取消 c。
- paths.json 阶段3 例子重写为完整可运行代码(含两个对照),展示代码原样提取运行通过;变式答案引用实测轨迹;修掉原"gather 不取消兄弟=一定后台跑完"的过度概括(显式区分 gather 语义 vs 运行器收尾)。全程 asyncio.sleep。

### 1.4 LP-003/LP-033 措辞 ✅
- LP-003:「再试结果一样/重试必失败」→ 工程策略表述(回喂不改变触发条件,按终态处理;截断修法=调预算)。
- LP-033:「填的人晕,系统校验慢」「深嵌套的问题」→ 条件化(常见后果/程度取决于模型任务 schema,决策前自评)。
- 全库扫描:残留清零;lp003_mock 18 项确认不回退。

## 验证状态(阶段1 验收门)

- node tests/behavior-tests.js:81/81
- python tests/lp003_mock_test.py:18/18
- python tests/path_exercise_test.py:19/19
- validate_bank:0 错 0 警;build 可复现;306 题
- 浏览器复验 ✅:注入 {done 早,cancelled 晚} → 路径页显示「⭕ 阶段 1 · 已取消确认(时间)」、进度 0/6、无 NaN;重新确认→done(时间戳更新);再取消→done 留痕+cancelled;刷新后取消态保持;阶段3/6 练习页代码与变式显示正确;8765 真实数据完好(306/1 轮/笔记,无渲染错误无 NaN)

## 已知失败 / 待核查

- 无已知失败。阶段1 验收门通过。

## 阶段2:知识结构(部分完成)

- ✅ 全库扫描:前置环 0、题目引用断链 0、doc_refs 断链 0(脚本核对)
- ✅ data/concepts.json:18 个核心概念(稳定 id/通俗名/一句话定义/前置概念/关联题目与章节),三向断链检查通过;已打包进 data.js
- ✅ 题目页 relLinks 升级:新增「先懂这些概念」(前置题对应概念,悬停显示定义)与「本题涉及概念」两行——不再只显示题号
- ✅ 六个专题章节新增「学完本章」(应能解释/应能做出/常在哪出错)
- ✅ 浏览器复验:PY-004 显示「先懂这些概念:生成器(暂停/恢复) · 协作式并发(asyncio)」与「本题涉及概念:协作式并发/请求校验与422/流式响应」
- ⏳ 待做:概念别名搜索、按概念导航页、题目反向定位到原理段落(锚点级)

## 阶段3:内容批次核查(进行中)

- ✅ 结构级扫描(52 基础题):追问答案、check.explain、pitfalls 数量、白话边界说明——仅 LP-029 缺 check.explain,已补(检验点:混排语言边界样本意识)。
- ✅ 开头直给扫描:无铺垫式开头。
- ✅ EN-040 补 OpenAI 官方 API Reference 与 Rate limits 文档页来源(usage/SSE/429 逐条对应),状态保持 partial。
- ✅ 抽样通读 8 道(PY-001/005/009/010/011、FD-024/025/027):直接答案开头直给、检查题均为变式、来源/边界标注完整。
- ✅ 批次2:剩余 8 道全部处理——5 道补具体来源(OpenAI 多语言指引/Cookbook/W3C I18N/Rules of ML),4 道纯实践主题待核查项具体化;基础无 URL **4/52**(前两轮 28→14→9→4)
- ⏳ 待做:进阶/高级题按主题批次(每 10~20 题)通读。

## 阶段4:练习题型扩展 ✅(结构)

- ✅ 六阶段各补 3 个专项练习(代码预测/找错修复/条件变化),共 18 个:每个含题目、参考要点、原因;先答再看(折叠),默认不自动标掌握
- ✅ 覆盖真实可运行场景:元组元素可变性预测、as_completed 顺序预测、gather 串行找错、gt=0 找错、评测脚本找错、SQL 注入找错、重试预算条件变化、max_rounds 条件变化等
- ✅ 与题号联动:每题变式注明对应题目概念(PY-001/PY-022/LP-003/AG-002/RG-055…)
- ⏳ 待做:做答输入框与复盘记录(当前为参考要点展开;做答记录走学习页笔记)

## 下一步

1. 阶段4+:练习题型(代码预测/找错修复/条件变化等)、动手项目 A-D、复习训练、面试表达。
2. 阶段3 批次2+:进阶主题批次通读。

## 阶段4 完成(专项练习)✅ + 阶段5 完成(动手项目)✅ + 阶段6 完成(复习训练)✅

- 阶段4:六阶段各 3 个专项练习(代码预测/找错修复/条件变化)= 18 个,先答再看;浏览器验证渲染(18 个 + 3 类徽章)
- 阶段5:projects.json 定义 A/B/C/D 四个项目;projects/proj_a~d 四份可运行代码全部实测:
  - A:六场景(正常/拒答/schema回喂/临时退避/业务终态)输出与预期一致
  - B:取消在 await 点感知、finally 清理、chunk≠token
  - C:评测 5/5(recall 4/4 + 拒答正确);阈值/top_k 演示
  - D:三步任务+检查点恢复+类型错误/未知工具分支
  - 路径页新增「动手项目」卡片区(4 个,含运行命令/预期/排查/扩展/完成标准)
- 阶段6:复盘原因 6 类多选(record.reviewReasons,备份取并集)、复习队列「未消化」优先排序(浏览器实测 PY-003 带原因排 PY-001 前)、间隔提示为启发式不覆盖手动状态

## 下一步

1. 阶段7:搜索增强(概念命中/练习命中/项目记录命中)。
2. 阶段8:面试表达(30 秒版/2 分钟版字段已有 interview;补模拟追问梯度)。
3. 阶段9-11:可访问性细查、备份完整性走查、最终交付文档。

## 阶段7(搜索统一)✅ + 阶段8(面试表达,部分)✅

- 搜索索引纳入概念(18)与动手项目(4):搜「协作式并发」命中概念卡(跳 PY-003)、搜「流式响应与取消」命中项目卡(跳路径页);结果徽章与字段标签修正;currentCtx 统一(所有重建点带 concepts/projects)
- 模拟面试页加「追问梯度」提示条(概念→原理→代码→边界→排查→取舍)
- ⏳ 待做:概念导航页、面试 30 秒版/2 分钟版结构化、可访问性细查、备份完整性走查

## 当前状态

- 提交:ff89950 之后本地新增阶段7/8 改动(未提交)
- 测试:81/18/19 全过;validate 0 错 0 警;dedup 无预警

## 阶段9-11:完成 ✅(范围见最终报告)

- 备份完整性:drillsOpened 等 ui 偏好进备份,合并规则「本地为空才采用」(Node 验证恢复)
- 最终交付:docs/upgrade-final-report.md

## 总结

任务书 12 个阶段中:0-8 全部完成、9-11 完成可执行范围、遗留项见最终报告"未完成/风险"节。

## 阶段3 批次3:进阶/高级全量结构扫描 ✅

- 全库 306 题结构级清零:EN-031/EN-032/FD-009/RG-062 补 check.explain(检验点:超窗三层/分帧协议/外推区实测分寸/trace 归因顺序)
- 内容抽读 10 道(RG/AG/EN/AD):无铺垫开头;3 道 check 误报经人工确认为合格变式(trace 归因/矛盾诊断/趋势解释)
- 检查题变式信号词表补充(trace/矛盾/趋势类问题同样是变式)

## 阶段8 完成 ✅(概念地图 + 项目表达模板)

- 概念地图进路径页:18 概念按主题分组、30 秒版定义、点击跳题目(浏览器验证 18 组全渲染)
- 项目表达模板:需求/方案/取舍/问题/验证/不足 + 诚实分级(做过/练手验证过/会设计,不混用)
- 排错记录:测试服务器进程死亡导致 fetch 失败,重启后确认(与代码无关)

## 阶段1.2 完成 ✅ + 阶段2(项目做实)✅

- 1.2 笔记时序:每次击键同步进内存(Store),磁盘防抖;同页状态/收藏/修订确认/换题前 captureNote;浏览器实测快速时序(输入→立即点状态→150ms/700ms/刷新三检查点文本框/内存/存储全为新值)
- 2.1 项目D:状态链真实传递(10°C→50.0°F 写真实 report.txt);0°C→32.0°F(0 不当缺失);缺温度如实报错;检查点续跑只补 2 次调用;5 项断言
- 2.2 项目A:回喂真的改变请求(FakeModel 校验第 2 次请求必须含『上次错误』,无反馈不放行——反向证明);补截断终态分支;六场景 8 项断言(tests/proj_a_test.py);目标层级诚实化(最小教学骨架/验证版/工程扩展)
- 2.3 项目C:拒答原因分 2 层(零命中=可能检索盲区,低分=证据不足);变式实验真实可触发(『退货』vs『退款』对照;top_k 2→1 裁证据);评测逐条明细(query/候选/采用/引用);样例集标注只证明流程
- 2.4 projects.json 与实际行为同步(目标/预期/排查/扩展/完成标准/层级)

## 阶段3+4:统一学习记录与首条验收链 ✅

- 18 专项练习稳定 ID(drill-<阶段><类型><序号>,如 drill-1pred01)+ version 字段;不再依赖数组下标
- Store 新增 drillTries 尝试记录:myAnswer/observed/selfRating(枚举含空)/review/mistake 预留/version/ts;类型校验;备份合并按 (drillId,ts) 幂等追加
- PathView 专项练习升级:先答(草稿=ui.drillDrafts,刷新不丢)→ 揭示参考 → 记录观察/自评/误解原因 → 保存尝试(历史保留);离开返回历史在
- 修复 3 个实现 bug:自评按钮选择器错位(data-rate vs data-drill-rate)、selfRating 空串校验、data.js 未随 paths.json 重建(SW 旧缓存连锁)
- **首条验收链全通**(浏览器实测):PY-001 概念 → 写预测 → 揭示参考 → 记录观察/自评 partial/复盘 → 保存(drillTries 落盘)→ 离开返回历史保留 → 清空+导入备份后尝试记录与复盘完整恢复

## 阶段6 补齐:复习原因取消语义 ✅

- 修复「取并集导致已取消原因永远复活」:合并改为按记录级 _updatedAt 取整套覆盖(当前状态);旧备份只补空不清空
- 3 项断言入 behavior-tests(总 93 项)

## 阶段7 补齐:搜索覆盖专项练习 ✅

- drill 题面+参考+原因进索引;搜索结果跳路径页并显示专项类型/题面;徽章「专项练习」
- currentCtx 统一提供 drills(concepts/projects 同前)
- ⏳ 待做:个人尝试记录(myAnswer/review)进索引并定位到具体尝试;项目个人记录进索引

## 阶段7 完成 ✅(搜索统一,浏览器验证)

- 索引覆盖:题目全部层级/文档/概念(18)/专项练习题面+参考(18)/个人尝试记录(myAnswer/observed/review)/项目说明(4)
- 浏览器实测:搜「冻结的是槽位」(仅存在于我的复盘文字)→ 命中「我的尝试」卡;搜「元组槽位不可写」(仅存在于练习参考)→ 命中「专项练习」
- 结果徽章与跳转修正;currentCtx 统一;heredoc 转义引发的两处语法错误已修
- ⏳ 待做:个人记录命中跳转到具体尝试(当前到路径页);项目个人记录区

## 阶段9 完成 ✅:备份 round-trip(真实数据)

- 用真实题库数据(PY-001 原题)构造完整状态:笔记/收藏/状态/reviewReasons/drillTries/轮次/阶段取消态/阅读位置/扩展题/资料
- 10 项断言:全部恢复、drillTries 幂等、阶段取消态逐字恢复
- 行为测试总量:103 项全过
