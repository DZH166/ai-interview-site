# 本轮交付:记录可靠性与练习闭环(2026-09-14,任务书阶段0~10)

> 后续复审发现组合边界未覆盖，并已继续实修。当前结果、768项验证与尚未覆盖边界见[复审修复与后续升级](复审修复与后续升级-2026-09-14.md)；以下为原阶段交付记录。


基线:`944764b`(审查对象 `d264959`)。本文件是本轮唯一交付总结;状态口径见[问题状态表](问题状态表-记录可靠性.md)。

## 已修问题(8/8 关闭,均有「未修复版本失败→修复后通过」证据)

| 编号 | 问题 | 关闭阶段 | 关闭证据 |
|---|---|---|---|
| ST-01 | 无内容变化仍跨页写盘/重绘/抢焦点 | 阶段1 | `browser/remote-sync.js` ST-01a~g(静置3秒计数稳定);`remote-merge-test` 4b 变更语义 |
| ST-02 | 旧DOM覆盖另一页已保存笔记 | 阶段1 | `remote-sync.js` ST-02a~l(dirty 基线+冲突处置,两版可找回) |
| SP-05 | 清空被另一页复活 | 阶段2 | `remote-merge-test` 4c 纪元门控矩阵;主动恢复对照 |
| SP-06 | 完成会话恢复为草稿 | 阶段2 | `remote-merge-test` 4d sessionId 终态守卫+同会话 LWW+残留收敛 |
| SP-01 | 旧备份复活已撤销 SRS | 阶段3 | `srs-test` 4b suggestable;补回限全新记录;三入口一致 |
| SP-04 | 未到期显示「已到期」 | 阶段3 | `srs-test` 4 纯剩余时长语义(23h/1ms/同毫秒边界) |
| SP-02 | 追问按下标错配 | 阶段4 | `browser/followup.js` 身份=内容哈希:重排跟随原题面/改写待核对/题面快照 |
| SP-03 | 只答追问不能导出 | 阶段5 | `express-card-test` 第10节;完成页/首页/历史同一资格定义 |

复现证据存档:`docs/验收快照/阶段0-问题复现-944764b.txt`;基线复现用例的断言已并入正式套件,`tests/baseline/` 已删除。

## 升级功能(阶段6~9)

- **Anki 出口契约**(阶段6):4 列 CSV + `#notetype/#deck/#guid column:2`,同题号二次导入按更新合并;
  可下载导入指南;诚实标注「真实 Anki 桌面版导入未在本环境验证」;样例 `examples/anki-import-sample.csv`。
- **搜索内容版本失效**(阶段7):静态层签名 = build.py 内容哈希+题库规模+文档集;等长替换触发重建,
  笔记编辑不触发;我的追问回答进索引,深链展开到具体轮次。
  性能实测(349题):全量重建 5.3ms / 动态 5.5ms / 查询中位 1.6ms——倒排与 Worker 明确不做。
- **内容可解释性**(阶段8):14 题量化口径声明(区分算术/示例/论文实测/未验证经验);
  13 道融合新增题经适配器输入实补(哈希守卫未绕过);审计提醒 141→113,硬问题 0;
  剩余 55 题薄项入 `delivery/内容修订工作清单.json`(阈值未降,`tools/gen_thin_list.py` 可再生)。
- **存储口径校准**(阶段9):维护页明示两种口径(序列化字符数 vs origin 配额估计);
  极端压力实测 1.55MB/写入20ms/往返39ms(`delivery/reviews/stage9-storage-measure.json`)。

## 迁移兼容性

- 新增字段(`resetEpoch/resetTs/mock.ended/sessionId/srs/followups[].id/q/qRev`)全部可选;
  `validateRecordsObj` 校验其类型;旧备份、旧格式追问草稿(下标键)有迁移与「待核对」降级,不猜测映射。
- 清空纪元对旧备份的影响:主动恢复 = 用户明确意图,纪元不回退(有测试)。

## 实测结果(阶段10 完整回归,实跑统计)

- **Node 384 + Python 111 + 浏览器 259 = 754 项,0 失败**(`docs/验收快照/阶段10-完整回归-最终.txt`)。
- 工具链:validate_bank 0 错 0 警;dedup 无预警;content_audit 自检通过;verify_sources 结构枚举通过;
  fusion/pi-agent `--check` 通过;构建两次运行逐字节一致;shell 缓存戳一致。

## 未验证项(如实)

- 真实 Anki 桌面版导入(环境无 Anki;已给样例与指南,首次导入需按指南核对一张卡)。
- 真实读屏软件、低端设备存储与隐私模式配额;输入法组合输入在双页冲突场景的行为。
- 多于两页同时高频写入(当前验收覆盖双页矩阵)。
- 55 题剩余「薄」项与来源逐条联网核验(留后续内容批次,清单已备)。

## 提交与发布

提交序列(每阶段一提交):阶段0 `dea371c` → 阶段1 → 阶段2 → 阶段3 → 阶段4 → 阶段5 → 阶段6 →
阶段7 → 阶段8 → 阶段9 → 阶段10(本提交)。推送后 CI(validate+browser 两 job)与 Pages 部署需与本 SHA 对齐后核对线上。
