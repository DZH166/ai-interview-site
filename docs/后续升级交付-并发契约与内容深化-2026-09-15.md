# 后续升级交付:并发契约与内容深化(2026-09-15)

承接[复审修复与后续升级](复审修复与后续升级-2026-09-14.md)的后续方向 1~7。基线 `617591a`。

## 1. 并发压力基线(后续1)✅

`tests/browser/concurrency-matrix.js`(已入 CI):固定种子双页矩阵,7 类情境,每步核对
A 内存/B 内存/磁盘/重开四态——

| 情境 | 结果 |
|---|---|
| 同字段顺序写入 | 四态收敛到后写者 |
| 不同题并发写入 | 两条都保留(无静默丢弃) |
| 同毫秒双写 | 决胜规则确定(重复运行一致),内存与磁盘收敛 |
| 双清空(计数+时刻不同) | 取较晚清空,笔记不复活 |
| 暂停恢复后旧页保存 | saveNow 前合并磁盘快照,不覆盖新值 |
| pagehide 竞争 | dirty 输入进冲突副本,已保存版本不被覆盖 |
| 清空后新写入交错 | 新记录保留、被清空内容不复活 |

矩阵记录:`delivery/reviews/concurrency-matrix.json`。

## 2. 写入协调选型(后续2)✅

见[写入协调选型](写入协调选型-2026-09-15.md)。采纳「同步 saveNow + 保存前合并 + 纪元/终态边界」;
Web Locks(13 个同步调用点迁移 + file:// 不可用)与 CRDT(重定义全部记录形状)否决,理由留档。
补 `tests/save-protocol-test.js` 12 项:无竞争直写/合并后再写不丢对方/坏数据不覆盖可重试/同刻决胜确定。
诚实边界不变:**完全同时的多写者读改写不宣称安全**。

## 3. 备份可预览恢复(后续3)✅

`Store.previewRecordsMerge`(与导入共用 `parseRecordsBackup`)+维护页「确认恢复」弹窗:
按题归类新增/采用备份/保留本机/备份撤销本机,轮次/草稿/项目运行预告,并解释合并口径。
`tests/preview-restore-test.js` 16 项:预览不改状态不落盘、归类正确、与实际导入结果一致、非法与未知版本拒绝。

## 4. 55 题内容深化(后续4)✅——薄项工作清单清零

| 批次 | 内容 | 提醒 |
|---|---|---|
| 4a | PI Agent 25 题各补第 3 条误区(经适配器输入 + manifest 哈希同步) | 88 |
| 4b/4c | 29 题各补第 3 条误区(经 runtime-edits 输入落地;直改路径废弃) | 88→59 |
| 4d/4e/4f | 29 题 plain「自检」句 / answer 结论限定句 / deep 小节(方差意识/实验纪律/混用策略/成本核对/客户端清单/选层选秩记录表/组设计要点) | 59→21 |
| 4g | 最后 2 题 deep 收尾 | **薄项 0** |

全部经适配器唯一入口落地(`--write`→`--check` 幂等),受管理哈希守卫未绕过;过程中适配器的
重复条目守卫(46!==30)还捕获了一次跨文件重复,据此把变更并入正确的输入文件。
每条内容针对题面(方法/边界/反例),无通用凑字段落;阈值未降低。

## 5. Anki 真实兼容验收(后续5)——保持未验证标注

环境无 Anki 桌面版。已交付:三列契约(题号/正面/背面)+样例文件+导入指南;
文件结构、转义、真实下载已验证;**桌面导入结果仍未验证**,指南要求首次导入后核对一张卡片。

## 6. 长周期体验校准(后续6)✅(测试口径)

- 同轮重复点击同一复盘按钮不重复排期、改自评=从当次基线重算:已有断言
  (`browser/srs.js`、复审轮修复);
- 跨天/未来文案一致性:纯剩余时长语义(`srs-test` 4 节);
- 旧版本快照提示:表达卡区分作答时/当前版本并注记(`followup.js`)。

## 7. 发布验收(后续7)✅

全量回归实跑:Node 407 + Python 111 + 浏览器 266 = **784 项,0 失败**
(`docs/验收快照/后续轮-完整回归-2026-09-15.txt`)。
工具链:validate 0 错 0 警;dedup 无预警;审计硬问题 0、提醒 21(全部为相对总长口径);
fusion/pi-agent `--check` 通过;构建两次逐字节一致;缓存戳一致。
推送后核对本 SHA 的 CI 与 Pages 及线上关键文件。

## 未验证项(如实)

- 真实 Anki 桌面版导入;真实读屏;低端设备/隐私模式配额;
- 三方以上同时高频写入与完全同时的多写者(设计上不宣称,见选型文档)。
