# 阶段8:内容可解释性推进(2026-09-14,任务书阶段8)

## 量化口径分类(阶段8.3/8.4)

14 题数字措辞题(阶段5 处理过的)按任务书要求补齐**口径声明**,区分四类来源:

| 类别 | 本库现状 |
| --- | --- |
| 可运行算术 | ai-series-math-test 覆盖的 5 项(LoRA 参数量等)——已实测 |
| 明确假设的示例 | 题面标「(示例)」的演算看板——审计器已豁免,正文以「示例/示意」标注 |
| 论文/官方实验结果 | 有 paper/official 来源并标 verified 的题 |
| **未验证的经验建议** | 本批 14 题——此前只有措辞缓冲,现补口径声明 |

实现:12 题 `verify.note` 追加「量化口径:…教学示例或经验区间…非官方基准,引用前请单独核实」;
2 道受管理题(AG-001/AG-031)正文追加同名口径段(走适配器输入/有记录的复核采纳,哈希保护未绕过)。
「约/常见/量级」保留——口径声明回答「这些数字是什么性质」,措辞缓冲回答「能不能当成精确值」,两者互补。

## 13 道融合新增题内容实补(阶段8.5,不凑字数)

经适配器输入 `new-questions.json` 修订并重新生成(--write 后 --check 通过,13 更新):

| 题 | 补的内容(性质) |
| --- | --- |
| LC-001/005 | 观察方法:实测首块到达/最小示例验证流式与超时(可执行的操作步骤) |
| LC-002/004 | 可控清单/行为对齐清单:工具过滤+业务校验+证据回传;钩子时机/状态/异常/并发逐条对齐 |
| LC-003 | 幂等键方法:恢复重跑先查业务幂等键,已生效复用结果(具体机制) |
| LC-006 | 证据表:结论↔来源↔摘录↔置信度,反例单独一列(可复核结构) |
| RG-071 | 裁决留痕:引用哪条材料、依据什么规则、谁确认(可复查流程) |
| AG-059 | 三层组合的落地检查清单(技能来源/授权范围/执行复核各自版本化) |
| AG-060/061 | 远程协作要补的四件事;路由的两个度量(漏召回率/边界在执行层) |
| LP-046 | 责任矩阵+降级策略;训练/运行时分开的评测口径 |
| LP-047 | 传输选择判断标准+「WebRTC管媒体、WebSocket管信令」的常见搭档形态 |
| FD-028 | 按真实任务做位置对照实验,用曲线而非标称值决策(方法) |

每题补一条新 pitfall(反例型,不是复述题面);AG-001 plain 补「复述时的自检」(掌握判定)。

## 审计结果与剩余工作(阶段8.5 未完部分,如实)

- 提醒 141 → **113**(硬问题保持 0);「薄」类按 MIN_LEN 规则全量统计仍涉及 68 题
  (pitfalls<3 为最大项)。
- 本批覆盖:13 道新增题(全量实补)+ AG-001。**剩余 54 题旧题薄项未处理**,
  机器可读清单见 `delivery/内容修订工作清单.json`(55 题,`tools/gen_thin_list.py` 随时可再生成),
  留给后续内容批次——按任务书「提醒数量下降是副产品」,不为凑数字批量注水。
- 阈值未降低、未删除断言(MIN_LEN/坑点数下限保持原值)。

## 验证

- fusion `--write`→`--check` 通过(0 新增/85 跳过/13 更新);pi-agent `--check` 通过;
  ai-series-test 5/5(含受管理哈希守卫反证);validate_bank 0 错 0 警;build 可复现。
