# 问题状态表:记录可靠性与练习闭环(唯一口径)

> 本表随阶段推进**原地更新**。基线:`944764b`(审查对象 `d264959`)。
> 复现证据:[阶段0-问题复现-944764b.txt](验收快照/阶段0-问题复现-944764b.txt) ·
> 机器可读:[baseline-944764b.json](../delivery/reviews/baseline-944764b.json)
> 基线测试实测:Node 333 / Python 109 / 浏览器 226,合计 668,0 失败(工具链:0错0警、审计0硬问题、构建可复现)。

| 编号 | 问题(一句话) | 严重度 | 状态 | 关闭证据落点 |
|---|---|---|---|---|
| ST-01 | 无内容变化仍跨页写盘、重绘并抢焦点(storage→App.route 循环) | P1 | ✅ 已关闭(阶段1) | tests/browser/remote-sync.js ST-01a~g;remote-merge-test 4b(变更语义) |
| ST-02 | 旧笔记控件(未编辑)覆盖另一页已保存的新笔记 | P1 | ✅ 已关闭(阶段1) | tests/browser/remote-sync.js ST-02a~l(含冲突处置:两版均可找回) |
| SP-01 | 旧备份恢复已撤销的 SRS(三入口一致;跨页路径 lapses 还会累加) | P2 | ✅ 已关闭(阶段3) | srs-test 4b(suggestable 有效性);baseline SP-01a~c 转绿;mergeQuestionRecord 补回限全新记录 |
| SP-02 | 题库更新后旧回答静默绑定到另一道追问(追问无身份) | P2 | ✅ 已关闭(阶段4) | browser/followup.js 16条(重排跟随原题面/改写待核对/题面快照);baseline SP-02a 转绿 |
| SP-03 | 只写追问的真实练习无法导出表达卡 | P2 | 🔴 复现未修 | 阶段5 |
| SP-04 | 未到期(23h/1ms 后)的排期提示显示「已到期」 | P2 | ✅ 已关闭(阶段3) | srs-test 4(纯剩余时长语义,先判到期再分档);baseline SP-04b/c 转绿 |
| SP-05 | 清空全部记录后被另一页复活(无清空版本边界) | P1 | ✅ 已关闭(阶段2) | remote-merge-test 4c(纪元门控矩阵);baseline SP-05a 转绿 |
| SP-06 | 已完成会话恢复为未完成草稿(无 sessionId/终态) | P1 | ✅ 已关闭(阶段2) | remote-merge-test 4d(终态守卫+同会话LWW+残留收敛);baseline SP-06a/b 转绿 |

## 基线记录外的两个测试环境事实(影响后续复现方式)

1. **SP-05/SP-06 的前置断言必须同步执行**:ST-01 的循环会在静置窗口内把清空/完成状态写回
   ——异步前置检查混入同一缺陷家族,不能作为隔离前提。
2. **SP-02 的响应替换必须阻断 Service Worker**:SW 内部的 fetch 不经过页面级路由,
   `serviceWorkers:'block'` 的独立上下文才能模拟「线上内容升级」。

## 复现用例的归宿

`tests/baseline/` 下的两个复现文件**不进 CI**(未修复版本上必然红)。
对应阶段修复后:断言并入正式套件(`srs-test` / `express-card-test` / 新浏览器套件),
`tests/baseline/` 目录删除,本表状态改为 ✅ 并附提交号。
