# 阶段:加固与出口补强(2026-09-13)

## 内容

| 项 | 说明 |
| --- | --- |
| CSP 兜底 | `index.html` 增加 `Content-Security-Policy`(script-src 'self',style 需 unsafe-inline——站内大量 style= 属性)。防导入题库/资料内容注入脚本。配套:`javascript:void(0)` 链接(CSP 下会被拦)改为真按钮(共 3 处,原生键盘可达,语义更对);内联 SW 注册脚本移入 `app.js`(内联脚本会被 CSP 拦) |
| Anki CSV 出口 | `ExpressCard.buildAnkiCsv`(纯函数):与待攻克清单同一数据源,`#separator/#html` 指令头,内容先 HTML 转义再换 `<br>`(导入 Anki 渲染安全);首页「待攻克清单」卡新增按钮,`exportAnkiCsv()` 接真实下载(带 BOM,Excel/Anki 都能读) |
| 死代码清理 | 工作台首页的 `randomQid` / `#h-random`(按钮早已不存在)删除 |
| 假日期诚实化 | `build.py` 里硬编码的 `generated_at` / `built_at` 改为数据**内容哈希**(`content_hash`,确定性,构建可复现)——之前那两个日期既不是构建日期也不是数据日期 |

## 过程中发现并解决

- 阶段 5 的措辞修订触及融合适配器的**受管理题目** AG-031,`fuse-ai-series.js --check`
  按设计报「Managed question was edited; review before overwriting」。按其流程做了一次
  「复核后采纳」:确认修订内容(数字断言清偿的措辞),重算 `output_hash`,
  融合清单同步,`--check` 恢复通过。这验证了哈希守卫真的能拦住未审查的直接修改。

## 测试

- `tests/express-card-test.js` 扩至 56 条(+9):Anki CSV 指令头、题干/答案/误区三段、
  HTML 转义(原始引号不进入字段)、无标记拒绝、文件名。
- `tests/browser/hardening.js`(新,8 条):CSP meta 存在、**全程无 CSP 违规**、
  Anki CSV 真实下载(Playwright download 事件 + 内容断言)、SW 注册代码在 app.js、
  index.html 无内联脚本、全程无 JS 异常。
- 全量回归:Node 335 / 浏览器 226 条全绿;fusion --check / pi-agent --check 通过;
  validate 0 错 0 警;build 可复现。

## 未做(如实)

- 语音口述自答(Web Speech)未做:需麦克风权限,与「纯本地可测」的测试纪律冲突,留待真机验证后再上;
- 模拟面试内追问计时未做:现有 30 秒/2 分钟计时已在项目口述处,推广属于体验优化;
- CSP 仅覆盖主页面:表达卡打印版(Blob 新窗口)不受主页面 CSP 影响,其内容已全量转义。
