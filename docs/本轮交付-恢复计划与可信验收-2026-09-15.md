# 本轮交付:恢复计划与可信验收(2026-09-15,任务书阶段0~9)

基线:`147a179`(审查对象 `ec4b327`)。审查报告 8 项问题(3 Standards + 3 Spec + 2 主审补充)全部关闭。

## 修复清单(每项均有「未修复版本失败 → 修复后通过」证据)

| 编号 | 问题 | 修复 | 关闭证据 |
|---|---|---|---|
| MR-01 [P1] | 预览漏判旧runId迁移内容,显示「无需恢复」阻断恢复 | 阶段1 统一恢复计划生成器 `computeRestorePlan`:校验→**规范化(迁移前移)**→合并→差异→实体明细,预览与导入共用同一份流程 | baseline2 MR-01a/b/c 转绿 |
| MR-02 [P2] | 预览不告知将覆盖项目运行记录 | `describeRestoreEntities` 按实体列 新增/覆盖/清空/保留,项目运行带 runId+前后摘要+原因;迁移条目标注 | baseline2 MR-02b 转绿;阶段2 UI |
| ST-01 [P2] | 同毫秒顺序写不收敛(A内存≠磁盘) | 阶段5 `sameTieBreak` 同刻决胜=内容哈希大者胜(清空优先);adopt **收敛传播**(合并结果与磁盘不同即写回,否则双方各持己见永不收敛) | baseline2 ST-01a~d 转绿;save-protocol 第6节反向断言 |
| ST-02 [P2] | 暂停场景假构造(`__paused` 无人读取) | 浏览器无法移除已注册 storage 监听(target 阶段按注册顺序,实测拦截无效——如实记录);改 Node **双 Store 实例**等价模拟(独立内存+共享盘,暂停方不接收) | `stage4-pause-test.js` 10项:S2-pre 证明 B 真持旧内存,恢复后不覆盖 |
| ST-03 [P2] | 关闭测试读不存在字段(ui.conflictDrafts),未验证输入副本 | 改查真实落点 `questions[].noteDraft`;S3 场景:pagehide 落盘→磁盘含原文→正式笔记不被覆盖→清空→完整备份往返→副本找回 | stage4-matrix S3a~d |
| SP-01 [P2] | AD-001/002 误区错配(照搬Agent题) | 经 model-edits 改为本题反例:RAG/微调失败模式重叠;量化与KV口径不同 | 题库 diff;fusion --check |
| SP-02 [P2] | PI-025 混淆提示缓存与内容缓存 | 经 pi-agent 输入改述:提示缓存=计算复用不改输入;上下文整理/结果缓存单列(含 manifest 哈希同步) | pi --check;题库 diff |
| SP-03 [P2] | AD-004 混淆固定seed重现与跨seed方差 | 改述:固定seed用于重现;稳健性比较用预先指定的一组seed做配对比较(PyTorch 可复现性口径) | model-edits diff |

另:AG-010「需求过半用框架」改为关键约束逐条决策(任务书 6.5)。

## 升级功能

- **恢复计划契约**(阶段1/2/3):预览=将要执行的那份结果;确认时重算并比对本地基线
  (`Store.rev`+磁盘raw),基线变化弹「重新计算后的计划」再确认,不悄悄换结果;
  写盘失败保留可重试路径。
- **并发矩阵重建**(阶段4):前置条件真实(S2-pre 先证 B 持旧内存)、四态核对(含重开)、
  反向验证(禁用传播→分歧重现,证明测试有效);轨迹存档 `delivery/reviews/stage4-matrix.json`。
- **练后下一步建议**(阶段8):完成页「下一步」卡区分**事实**(本轮实际操作)与**推断的下一步**,
  附复习队列/再练/补追问出口;资格复用 `itemAnswered`,未作答不算完成证据。

## 内容质量(阶段6/7)

- 55 题语义验收表:`delivery/55题语义验收.json`(按 ID 列语义变化字段,缩进不计)。
- 剩余 21 项提醒逐条处置:9 项量化逐题声明口径(可复算算术/教学假设「此例假设」/无来源经验建议如实标注),
  12 项相对总长为全库分位口径(分位随全库漂移,内容已完整处保留理由)——均记录于各题 `verify.note`。

## 实跑测试结果

**Node 419 + Python 111 + 浏览器 275 = 805 项,0 失败**
(`docs/验收快照/复审2-完整回归-最终.txt`;Python 两个 OK 套件按审查口径各计 7 项)。
工具链:validate 0 错 0 警;dedup 无预警;审计硬问题 0;fusion/pi `--check` 通过;构建可复现;缓存戳一致。

## 未验证项(如实)

- 真实 Anki 桌面版导入、真实读屏软件、低端设备/隐私模式配额;
- 完全同时的多写者读改写(明确不支持,见[写入协调选型](写入协调选型-2026-09-15.md));
- 真实 LoRA 训练与 SDK 运行(SP-03 为方法审查,未跑实验)。

## 提交序列

阶段0 → 阶段1/2/5 → 阶段3/4 → 阶段6/6b → 阶段7 → 阶段8 → 阶段9(本提交)。
