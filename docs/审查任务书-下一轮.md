# 给独立审查 AI 的完整审查任务书

请对 [DZH166/ai-interview-site](https://github.com/DZH166/ai-interview-site) 完成一轮独立验收审查。本任务书自包含,只需仓库即可执行;可长时间分阶段工作,遇到需要主观判断的实现细节自主决策并继续,不要反复询问。

## 一、项目背景与审查对象

仓库 main 分支。网站是面向 AI 应用开发/Agent 方向实习校招的中文学习站,纯静态(HTML/CSS/原生 JS,无框架无构建打包),数据与界面分离:`data/` 源文件经 `tools/build.py` 生成 `app/data.js`;`app/js/` 是无模块化的脚本按顺序加载;用户数据全部在浏览器 localStorage(键前缀 `aiiv:`)。线上入口 https://dzh166.github.io/ai-interview-site/ ,实际资源在 `/app/`。

用户是在校学生,准备 AI 应用开发/Agent 方向实习校招,尚未系统学习算法。**学习优先级:理解原理 → 动手验证 → 面试表达。** 网站只供个人本地使用,正常学习不依赖账号、云数据库、付费 API 或在线模型。

审查对象提交:**`ebc424b`**(开始前先 `git log --oneline -1` 记录实际 HEAD;若已有后续提交,保留新工作,逐项判断下列问题是否仍存在,不要为复现旧问题回退)。比较基线(可选):`357625f`。

当前自述状态(由上一轮开发者报告,**必须独立核实,不得照抄**):
- 306 题、12 篇文档、六阶段学习路径、18 道专项练习(stable ID:drill-<阶段><类型><序号>)、四个本地 Python 项目(projects/proj_a~d)、18 个概念。
- 学习记录模型:顶层 `drillAttempts`(drillId → attempt 数组,attempt 含 attemptId/status draft|completed/版本/预测/观察/自评/复盘/时间戳),旧 `questions[qid].drillTries` 由 `migrateLegacyDrillTries(target)` 迁移(启动 load、importRecords、importFull 三入口)。
- 项目个人记录在 `ui.projectDrafts`(草稿)/`ui.projectRuns`(runId 历史);复习原因 `reviewReasons`(整套覆盖语义,非并集)。
- 测试:behavior-tests 124 项、LP-003 mock 18、路径练习 19、项目A 8、项目C 7;validate_bank 0 错 0 警。

## 二、执行边界

1. 所有故障注入、坏数据测试在隔离副本/独立浏览器上下文执行;**不得清空或覆盖用户真实浏览器数据**(用户真实数据在 8765 端口的 localStorage)。
2. 默认不提交、不推送、不部署;交付源码差异、验证证据与清单,由用户决定发布。已授权的会话按已授权范围执行。
3. 不重新采集题库、不机械扩题、不引入新框架/账户/云服务/付费 API;fake model 明确标为替身。
4. 不编造用户经历、公司真题、性能指标。示例数字标注为示例;计算与机制必须正确。
5. "测试绿"不等于功能完成:每项验收要实际走浏览器或可复现流程,区分"字段存在""按钮渲染""数据持久化""用户可感知"。
6. 每阶段保存进度(建议写 `docs/` 下独立审查报告),中断可恢复;不要把审查写成一次性清单。

## 三、上一轮审查发现且声称已修复的问题(本轮必须逐项复验)

上一轮以这些问题为入口做了修复,声称全部完成。**请逐项独立验证修复是否真实、是否引入新问题:**

1. **专项输入导致启动崩溃**:曾因 search.js 引用未定义的 `NL` 常量,输入即抛 ReferenceError、刷新后主内容区空白。声称已定义 NL。验证:空用户、含旧 drillTries、含新版草稿/历史三种状态下打开站点、输入专项、刷新、搜索。
2. **项目保存一次产生多条记录**:曾因 `wireProjectRecords(root)` 在专项 forEach 循环内重复绑定监听,单击保存产生 18 条内存记录且未落盘。声称已移出循环、保存恰好 1 条带 runId 的记录并立即落盘。验证:连续保存两次、重渲染后再保存、刷新后数量不变。
3. **项目状态读写错位**:写 `ui.projectDrafts` 但读 `ui.projectRecords`。声称已统一。验证:步骤状态高亮刷新后保留。
4. **完整备份漏恢复项目草稿**:先访问路径页(自动建空壳)再恢复备份,草稿被跳过;本地 A 阻止备份 B。声称改为字段级合并(projectDrafts)+ runId 幂等(projectRuns)。验证:先访问路径→恢复、本地 A+备份 B、同 runId 重复导入。
5. **迁移入口不一致**:importRecords 曾不传目标对象导致迁移写错位置、启动 load 未迁移。声称三入口(启动/importRecords/importFull)统一且作用于待提交副本。验证:旧 `questions[qid].drillTries` 在三个入口都迁入顶层且旧字段迁出、重复运行幂等。
6. **搜索定位被回顶覆盖**:带 `?d=drillId` 的路由曾被 App.route 通用回顶覆盖,深位置目标看不到。声称 `#/path?d=` 已跳过回顶并滚动+展开+高亮。验证:页面后半段的 drill-5fix02(约 9000px 深处),不只测第一题。
7. **自评不入草稿**:自评按钮只改 CSS,刷新丢失。声称点击即写入当前 draft。验证:点自评→立即刷新→恢复。
8. **项目C 四层实验**:声称四层失败(库里没有/检索漏/证据预算/生成不遵循)均可真实触发并有断言(tests/proj_c_test.py);多证据问题 top_k=2 覆盖发票、top_k=1 缺失;错误 fake 被校验拦截。验证:运行项目入口与测试,检查"说明与代码行为一致"。
9. **保存/导入后的索引时效**:声称保存尝试、导入文档/记录、删除后索引立即更新,不需要手动刷新。验证:保存独特词→立即搜索;导入文档→立即搜到;删除→立即消失。

## 四、本轮审查重点(上一轮未覆盖或新引入的风险)

1. **数据模型一致性**:drillAttempts 成为顶层权威存储后,是否还有代码路径读写旧 `questions[qid].drillTries` 造成双写/漂移?attemptId 生成是否稳定(刷新/重复恢复不生成新身份)?
2. **合并规则边界**:projectDrafts 字段级合并的"本地空字段采用备份"规则,是否会覆盖用户主动清空的字段?reviewReasons 整套覆盖+updatedAt 语义在本地与备份时间相同、时间缺失时是否明确?
3. **UI 状态同步**:render 重建 DOM 后监听器重新绑定的完整性(上轮修过 projWired 守卫问题,检查是否有同类"守卫阻止重新绑定"残留);快速操作时序(输入→立即点其他按钮→刷新)下内存/磁盘/DOM 三方一致。
4. **错误语义**:存储成功但索引失败、存储失败但提示文案、导入部分成功的提示是否与真实持久化状态一致;不允许"实际保存了却提示失败"或反之。
5. **教学正确性抽查**:专项参考答案(尤其 tuple 增强赋值、count_words)、项目 A 回喂/D 状态链/C 四层的说明与代码实际行为一致;从展示字段提取代码执行,不以另一份正确代码背书。
6. **搜索与定位完整性**:索引覆盖题目全层级/文档/概念/专项/尝试/项目记录;命中目标路由携带真实身份;已删除内容不再被搜到;重复恢复不产生重复索引条目。
7. **移动端与可访问性**:360/390px 下专项自答、项目记录、历史弹窗、长代码;键盘可达性;提示文案与真实结果一致。
8. **内容质量抽查**:题目直接答案/大白话/原理/例子/误区/检查的互相一致性;来源 URL 与所支撑结论的对应;verified/partial 标签是否有事实依据。

## 五、验证工具与基线命令

先读脚本再运行;逐个 `app/js/*.js` 做 `node --check`:

```text
node tests/behavior-tests.js        # 124 项(含迁移/合并/round-trip/展示代码执行)
python tests/lp003_mock_test.py     # 18 项(结构化输出五类失败,桩客户端)
python tests/path_exercise_test.py  # 19 项(路径展示代码运行+轨迹)
python tests/proj_a_test.py         # 8 项(调用层回喂/六场景)
python tests/proj_c_test.py         # 7 项(四层失败)
python tools/validate_bank.py       # 题库结构校验
python tools/dedup_check.py         # 重复度
python tools/build.py               # 构建(产物与源数据一致性受 CI 监督)
python tools/serve.py               # 本地服务(实际输出的地址)
```

补充审查自己的回归:上一轮审查发现的每类问题都应有对应防线;上轮报告中"恒真断言"(`|| true`)类问题是否清零。CI(workflows/validate.yml)应覆盖 app/tests/projects 改动。

## 六、验收标准与交付

1. **逐项复验表**:上轮 9 个修复项,每项给出 独立复现步骤/实际结果/通过或失败;失败项给出定位(文件:行)与最小修复建议。
2. **新发现问题**:按严重度列出(启动崩溃 > 数据丢失/覆盖 > 功能错误 > 提示错误 > 体验),附复现步骤与证据(SHA、输入、实际输出)。
3. **数据安全确认**:全程未清空/覆盖用户真实数据;故障注入前后内存与磁盘快照对比。
4. **状态表**:区分"已实现且实测""已实现未实测""部分完成""未完成""未验证",不把字段存在当功能完成。
5. **测试评估**:现有测试是否真能挡住本轮故障;恒真/空 fixture/绕过 UI 的测试指出;CI 覆盖缺口。
6. **内容抽查结论**:抽查的题目/练习/项目清单、发现的教学错误、来源核查真实性。
7. **未验证边界**:真实手机、跨浏览器、PWA 升级、真实 API、PDF 流程等如实列出。
8. **修复优先级建议**:哪些必须下一轮先修,哪些可延后。

审查中如需修复少量阻断性 bug 以便继续验证,允许在隔离副本修复并明确标注"审查代修";不要大规模重写或推倒既有结构。发现上一轮报告与事实不符的地方,直接指出,不需要委婉。

现在从阶段1(NL 崩溃复现)和阶段3(先访问路径后恢复)开始独立验证。
