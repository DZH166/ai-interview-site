# DESIGN.md — 面试加油工作台

本文件是本项目的设计规范。改界面前先读它；新增组件时，它应该能回答
「这里该用什么颜色 / 字号 / 间距 / 圆角」。

---

## 0. 这份规范从哪来

不是凭空写的。2026-09-17 做了一轮界面审查（33 张双视口截图 + 量化统计），
并用 [`VoltAgent/awesome-design-md`](https://github.com/VoltAgent/awesome-design-md)
里两个最对口的设计系统做参照：

| 参照 | 为什么选它 | 借鉴了什么 |
| --- | --- | --- |
| **Mintlify**（文档平台，reading-optimized） | 同样是「长文本 + 导航」的信息形态 | 三列文档网格；正文 16px / 行高 1.5 **绝不压缩**；等宽字体只给代码；全站只用两种字体 |
| **Linear**（极密信息界面） | 同样是「高密度 + 克制」 | 用**表面差 + 1px 发丝边框**承载层级而非阴影；单一强调色；展示级用激进负字距 |

两家的共同点是同一个词：**克制**。这恰是本项目原先最缺的 ——
审查统计发现：字号 **19 档**、圆角 **11 种**、hex 色 **77 个**、间距 **21 种**
（混着 5/7/18/22px 这类不在任何刻度上的随手值）。

**刻意不照抄的地方**：两个参照都要求引入自有字体（Inter / Linear Display）。
本项目是**零依赖纯静态站**（无打包器、无外部请求、可离线），所以坚持系统字体栈。
宁可在字距与字号节奏上找补，也不引入一个字体请求。

---

## 1. 设计令牌

### 1.1 色彩

语义变量定义在 `style.css` 的 `:root`，暗色覆盖在 `[data-theme="dark"]`。

| 令牌 | 亮色 | 暗色 | 用途 |
| --- | --- | --- | --- |
| `--bg` | `#f4f6fa` | `#0f172a` | 页面底色 |
| `--card` | `#ffffff` | `#1e293b` | 卡片 / 面板 |
| `--line` | `#e3e8f0` | `#334155` | 1px 分隔线（承载层级的主要手段） |
| `--text` | `#1f2937` | `#e2e8f0` | 主文本 |
| `--muted` | `#6b7280` | `#94a3b8` | 次级文本、元信息 |
| `--primary` | `#2563eb` | `#60a5fa` | **唯一**强调色：链接、主按钮、激活态、展开态左缘 |
| `--ok` / `--warn` / `--danger` | `#059669` / `#d97706` / `#dc2626` | 对应亮色变体 | 语义状态，**只用于状态，不用于装饰** |

**规则**

- 主色是稀缺资源：只给链接、主按钮、激活态、展开区块的左缘色条。
  不要用它做背景填充或大面积色块。
- 层级优先用 **表面差 + 1px 边框**表达，不要用阴影堆叠（学 Linear）。
- 语义色不混用：`--ok` 永远表示「已掌握/成功」，不做装饰绿。

### 1.2 重点标注三色（本项目特有，不可与上面的语义色混淆）

正文里的人工标注由 `data/highlights/*.json` 驱动，共三级：

| 级别 | 亮色底 | 暗色底 | 含义 |
| --- | --- | --- | --- |
| `key` | `#fff1b8` + **加粗** | `#4a3a10` | 核心结论，可直接背 |
| `term` | `#e2efff` | `#1d3757` | 关键术语 |
| `warn` | `#ffe0da` | `#4a1f1a` | 易错红线 |

**规则**

- 三级必须**底色不同且互可区分**，`key` 加粗、`term` 不加粗 ——
  这条有浏览器断言守着（`tests/browser/highlight.js`）。
- 打印时底色失效，改用**下划线样式区分**（见 `@media print`）。
- 标注覆盖正文的比例有上限（校验器 `DENSITY_WARN=35%` / `ERROR=50%`）：
  **满屏都黄等于没有重点**。

### 1.3 字号：11 档（原来是 19 档）

| 值 | 用途 |
| --- | --- |
| 11px | 微标签（来源种类、`+N` 计数） |
| 12px | 徽章、辅助说明、标签 chip |
| 13px | 次要正文、控件文字、区块小标题 |
| 14px | 控件标准（输入框、按钮）、卡片正文 |
| 15px | **主要正文**（`body` 默认值） |
| 16px | 品牌字 |
| 17px | 小节标题（h2 / h3） |
| 20px | 页面标题（h1） |
| 24px | 首页大标题 |
| 26px | 统计数字 |
| `.9em` | 行内代码（相对值，随正文缩放） |

**规则**

- **不要再引入 12.5 / 13.5 / 14.5 / 15.5 这类半像素档** —— 差异肉眼不可辨，
  只会让「加粗」不再成为强调信号。
- 正文字号不小于 15px，正文行高 1.65。压缩行高比压缩字号更伤可读性。

### 1.4 间距：4px 基

采用 `4 / 8 / 12 / 16 / 20 / 24 / 32` 七档。

**规则**：新写的 padding / margin / gap 必须落在这七档里。
不要出现 5px、7px、18px、22px 这类值（本轮已清理掉一批）。

### 1.5 圆角：4 档

| 值 | 用途 |
| --- | --- |
| 4px | 行内代码、微标签 |
| 6px | 小 chip、`rel-link` |
| 8px | 按钮、输入框、区块卡片、搜索条目 |
| 10px（`--radius`） | 大卡片（`.card`、面板） |
| 999px | 胶囊（徽章、状态按钮、筛选 tab） |

**规则**：同一组件族不要混用 8px 与 12px。胶囊一律 999px 且不柔化。

### 1.6 表格

`--line` 做 1px 内框，`th` 用浅底（`#f8fafc` / 暗色 `--card`）。
表格**不受正文行宽约束**（否则会挤成一团）。

---

## 2. 动效纪律（重要）

2026-09-17 之前，`style.css` 里有一整段叫「流畅性与可视化」，但大半是在**加**动效，
而用户反馈恰恰是「不流畅」。实测滚动其实满帧（4 个页面各采样 399 帧，掉帧 **0**），
说明卡的不是滚动，而是**交互反馈被动画淹没**。

**原则：只过渡「需要让用户看清状态变化」的属性，其余一律瞬时。**

**禁止**

- ❌ `transition: all` —— 它会过渡**所有**属性，包括布局属性。
  原先它挂在 13 类元素上，其中 `.q-item` 有 349 个实例。
- ❌ 列表项逐个 `animation` 入场（stagger）—— 每次筛选/搜索重建列表都要重放一遍。
- ❌ 卡片悬浮 `transform: translateY()` —— 逐项位移触发整块重绘。
- ❌ `max-height: 0 → 5000px` 做折叠动画 —— 每帧重算布局，且 5000px 只是
  「够用」的魔数，内容再长会被静默截断。折叠用 `display` 切换。
- ❌ 全局 `scroll-behavior: smooth` —— 长页面（路径页 9066px）每次跳转都「飘」。
  需要平滑就显式写 `behavior: 'smooth'`；程序化定位一律用 `instant`。

**允许**（这些确实在传达信息）

- ✅ 按钮 / 输入的 `background-color` / `border-color` 过渡（120ms）
- ✅ 视图切换淡入（只 `opacity`，不做位移）
- ✅ 折叠箭头的 `transform` 旋转
- ✅ 阅读进度条宽度、专题进度条宽度
- ✅ 首页统计数字入场（只 4 个元素）
- ✅ `.flash` 定位高亮（搜索跳转后指出「就是这里」）

---

## 3. 布局

### 3.1 容器与行宽

- 页面容器 `max-width: 1280px`，居中，左右 padding 16px。
- **正文文本块 `max-width: 44em`**（约 42 个汉字宽）。
  审查实测桌面原本一行排到 **81 个字符**，中文要来回扫行。
  表格、代码块不受此限制。
- 窄屏本来就是单列，行宽限制自然失效。

### 3.2 各页面结构

| 页面 | 骨架 |
| --- | --- |
| 首页 | Hero → 今天的三件事 → 两个出口卡 → 统计四宫格 → 学习状态 → 专题进度 / 推荐用法双栏 |
| 浏览 | 顶部筛选栏 → 左 380px 题目列表 + 右详情（双栏）→ 窄屏堆叠 |
| 学习 | 工具条 → 记录栏 → 元数据 → 标题 → 题干 → 关联内容（折叠）→ 区块列表 → 笔记 |
| 文档 | 左 270px 目录（sticky）+ 右阅读区 |
| 搜索 | 搜索栏 → 筛选栏 → 结果列表（单列居中，max 900px） |

### 3.3 首屏纪律

**用户打开一道题，第一屏应该能看到「答案与面试表达」的开头。**

这是 2026-09-17 降噪的硬指标。移动端正文起点从 **708px 压到 483px**（−32%），
手段是：控制区在窄屏统一收窄、关联链接折叠、元数据只留 4 个维度。

新增任何「放在标题上方」的元素前，先问：它值得占掉首屏空间吗？
能收进折叠区吗？能移到正文之后吗？

---

## 4. 组件

### 4.1 区块（`.q-sec`）

题目的内容分块，是整个应用出现频率最高的组件。

- 带 **3px 左缘色条**：收起时是 `--line`，展开时是 `--primary` ——
  全展开的长页面里，这是「读到哪一块」的唯一视觉锚点。
- 标题与正文之间有 1px 分隔线。
- 块间距 12px。
- `data-sec="answer"`（答案与面试表达）的标题底色偏蓝（`#eff6ff`），
  与其余区块区分 —— 它是这页的主菜。

### 4.2 折叠区（`.rel-box` / `.rev-changes-box`）

用原生 `<details>`。**summary 必须自报内容量**
（如「关联内容 · 1 个前置概念 · 4 道相关题 · 3 个原理章节」）——
不展开也能判断值不值得点开。

### 4.3 元数据行（`.q-meta`）

**只放 4 个维度**：专题 / 难度 / 题型 / 我当前的状态。
核查状态与标签不放这里：前者与「出处与核查状态」区块重复，后者已挪进该区块。

### 4.4 按钮层级

| 类型 | 用法 |
| --- | --- |
| `.btn-primary` | 一屏最多一个（主要动作） |
| `.btn` | 常规动作 |
| `.btn-small` | 工具条、卡片内动作 |
| `.btn-danger` | 破坏性动作（删除/清空） |

### 4.5 状态色（`.badge`）

胶囊形，11–12px。已有语义化前缀：`b-topic` / `b-diff-*` / `b-type` / `st-*` / `vf-*` / `b-tag`。
**不要新增无前缀的通用色类**。

---

## 5. 内容呈现

本项目的核心内容是**中文长文本问答**（单题正文均 3574 字符）。
让长文本可读、可扫、可跳，是这个界面的第一要务。

**规则**

- 一行一个语义单元。不要把「直接答案」和「面试表达」揉成一段 ——
  读者分不清哪句能直接说出口（融合卡的两段各带小标题，是刻意的）。
- 区块顺序按「面试时用得上的先后」排：先给能直接说出口的，再给支撑材料。
  最长的「原理拆解」放最后 —— 它独占全文 26% 字符量，是选读而非必读。
- 代码块用等宽字体 + 深色底；行内代码只有 `.9em` 的相对字号。
- 引用块用左边框 + 浅底，不用斜体（中文斜体可读性差）。

---

## 6. Do / Don't 速查

**Do**

- 用 `--line` 边框和表面差表达层级
- 让主色保持稀缺
- 折叠区在 summary 里报出内容量
- 长文本约束行宽
- 动效只用于「状态变了」的信号
- 新增视觉值前，先查本文件第 1 节有没有现成档位

**Don't**

- 不要引入半像素字号档
- 不要 `transition: all`
- 不要给列表项加逐个入场动画
- 不要用 `max-height` 做折叠动画
- 不要用主色做背景填充
- 不要在标题上方堆更多控制元素（先考虑折叠或后置）
- 不要为「好看」新增依赖（字体、图标库、CSS 框架）—— 零依赖是硬约束

---

## 7. 相关文件

| 文件 | 说明 |
| --- | --- |
| `app/css/style.css` | 唯一样式文件，令牌在开头 |
| `app/js/common.js` | `QRender`：区块、元数据、关联内容的共享渲染 |
| `app/js/highlight.js` | 重点标注渲染器（标注与题库原文分离） |
| `data/highlights/*.json` | 349 题的标注数据（3917 条） |
| `tools/highlight_audit.py` | 标注校验（逐字命中 / 密度 / 元字符） |
| `tests/browser/highlight.js` | 标注渲染断言（12 条） |
| `tests/browser/search-browse-fix.js` | 搜索 / 浏览缺陷回归（9 条） |

改完 `app/` 下的 shell 文件后，**必须跑 `python tools/build.py`**
（更新 `app/sw.js` 的 `CACHE_VERSION`，否则老用户 PWA 拿不到新资源）。
自查：`node tests/shell-cache-stamp-test.js`。
