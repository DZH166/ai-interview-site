# 修改说明(修复轮,基于审查版本 0498044)

- 日期:2026-09-07 · 性质:本地修复,未提交、未推送、未部署(按约定等你验收)
- 范围:`app/js` 全部 9 个文件、`app/css`、`app/index.html`、`data/questions` 21 个文件、`data/docs` 7 个文件、`tools/build.py`、`tools/gen_verify_report.py`、CI 工作流、新增 `tests/behavior-tests.js`
- 审查清单执行情况见《修复任务清单.md》;内容修订脚本(`tools/_patch_content_1~3.py`)保留作为改动明细

## 一、核心故障修复(A 组:复习与历史)

| 问题 | 修复 | 验证 |
|---|---|---|
| `MockView.startTodayReview` 不存在,点「开始复习」抛 TypeError | 新增 `MockView.startDirected(qids, label)` 定向复习:复用自测会话(先写后看/复盘/历史),只含目标队列 | 浏览器实测:标 PY-022 待复习→今日复习(1)→开始复习→「第 1/1 题 · 今日复习」→标基本掌握→完成→回复习页队列为空 |
| `renderRounds` 未定义,点「模拟面试历史」抛 ReferenceError | 实现 `renderRounds`:轮次列表(details 折叠)+每题的题目/我的回答/是否对照参考/复盘状态/时间;兼容旧格式 | 浏览器实测:1 轮记录完整显示,含「已对照参考」与我的回答原文 |
| 今日队列「做完自动消失」文案与规则不符 | 明确规则并改文案:标「基本掌握」移出;「还不熟/待复习」保留;不自动假定掌握 | 实测:标 ok 后队列 1→0;标 weak/review 的题保留在队列(逻辑由状态驱动) |

## 二、检索/定位/目录/继续阅读(B 组)

| 问题 | 修复 | 验证 |
|---|---|---|
| 章节搜索片段只显示标题(「…」) | 章节 unit 的 `raw` 改为标题+正文,`makeSnippet` 在原文定位命中窗口(正则定位避免大小写变换错位) | 实测搜「可哈希」:片段为「…元组**可哈希**所以能当字典键…」真实上下文,带 `<mark>` 高亮 |
| 题目/笔记结果丢失 `u.anchor`,打开后全部折叠 | 结果链接带 `?a=<field>`;StudyView 按锚点展开对应区块并滚动定位;`check` 锚点同时揭示答案;`note` 滚到笔记框 | 实测:`#/study/PY-022?a=deep` 打开后原理区展开、其他区保持折叠、定位准确;笔记命中 `?a=note` 同样定位 |
| 锚点协议不一致(TOC 传 `sec-N`,拼接时再加 `sec-`) | `jumpToSection` 统一归一化(剥离 `sec-` 前缀),TOC/搜索/阅读位置三种来源全部兼容 | 实测:TOC 点 sec-3 → y=678;搜索 `?s=sec-1` 正常;继续阅读 `?s=3` 正常 |
| `#view.onscroll` 无效(实际 window 滚动),阅读位置永不保存 | 全部滚动逻辑统一到 window:进度条、位置记录/恢复、锚点滚动;监听在视图切换时清理 | 实测:滚文档→docPos 落盘(secId 裸编号+y);离开再「继续阅读」精确恢复到同一位置 |
| **新发现**:`html { scroll-behavior: smooth }` 让 JS scrollTo 变异步动画,定位后立刻读 scrollY 得 0 | 程序化定位一律 `behavior:'instant'` | 实测:跳转后 scrollY=678 正确,docPos.y 正确 |
| **新发现**:SW cacheFirst 导致改版后页面持续加载旧 JS | `build.py` 缓存版本改为对全部 shell 文件内容哈希盖章(排除 sw.js 自身避免追尾);两次构建产物逐字节一致(已验证) | 实测:改代码→rebuild→刷新两代内拿到新版 |

## 三、导入校验与记录保护(C 组)

| 问题 | 修复 | 验证 |
|---|---|---|
| 校验器不记录同批 ID;fresh 只看编号不看错误;存储失败仍提示成功 | 完整 schema 校验:类型/枚举(topic/type/difficulty/verify/sources.kind)/必填嵌套(followups{q,a}、pitfalls、check{q,a})/同批与库存冲突/乱码/引用;仅「零错误」的题进入导入预览;`extraBankSave` 返回 false 时不提交、不提示成功 | 实测:报告复现用例(两条 ZZ-991)→整体拒绝 0 题写入,错误逐条列出;混合用例(1 好 1 坏)→只导入合法题,刷新后 307 题存在、坏题不存在 |
| 自测草稿只在内存,刷新丢回答 | 草稿(config/items/idx/answers/directed)同步落盘 `Store.saveNow()`;输入/对照/复盘/翻页即写;配置页显示「上次未完成」恢复卡片;完成/放弃才清除;pagehide/visibilitychange 兜底冲刷笔记与草稿 | 实测:写回答→翻第 2 题→刷新→直接回到第 2 题;翻回第 1 题答案原文在;恢复卡片信息正确;完成后草稿清除 |
| importRecords 无类型/版本校验;旧笔记覆盖新笔记;轮次重复追加 | 完整结构校验(逐字段类型)+版本检查;合并规则:fav 取或、计数/时间戳取 max、note/status 按记录级 `_updatedAt` 新者胜(无时间戳只补空不覆盖);轮次按内容稳定 ID 幂等去重;先整体校验后原子写入(localStorage 先写成功才换内存),失败即抛且状态不变 | 行为测试 24 项断言全过:旧备份不覆盖新笔记、坏字段整批拒绝且快照不变、重复导入轮次不重复、类型/版本不匹配明确报错 |
| 导出不含导入资料,资料无独立导出 | 备份三分:个人记录(aiiv-records)/题库与资料(aiiv-library)/完整备份(aiiv-full);各入口自动识别类型;题库资料导入带回滚(后写失败回滚先写键) | 实测:导出→清空→导入→收藏与笔记恢复;重复导入幂等;库备份导入幂等(0 重复) |
| **新发现**:`Data.init()` 不可重入(旧 byId 误判导入题为冲突,重复 init 丢导入题) | 判重改用本轮新建的 seen 集合 | 实测:导入题 ZZ-901 在多次 init 后仍在 Data.allQuestions 中 |

## 四、交互断点(D 组)

| 问题 | 修复 | 验证 |
|---|---|---|
| 首页专题链接 `#/browse?t=…` 不被消费 | BrowseView 消费 `t`(及 `qid`)参数,应用筛选后 replaceState 清理 URL | 实测:点 Python 专题行→44 题,下拉框同步 |
| 「本专题章节」链接 `doc-${topic}-1` 失效 | `Data.topicMainDoc(topic)` 按真实文档取 order 最小者;无文档则隐藏该行 | 实测:AG-001 → `#/docs/doc-agent-1`;全专题抽查正确 |
| study 路由清空 NavCtx,上一题/下一题跳出筛选 | 移除清空;`NavCtx.neighbors` 对不在集合的题回退全量 | 实测:浏览页筛 44 题进学习页,上一题/下一题仍按筛选走(位置 x/44) |
| 「全部状态」与「未练习」共用空值 | 新增 `__none` 选项,`apply` 单独处理 | 实测:标 1 题掌握后筛「未练习」=43 题,该题不在列表 |
| 浏览详情「查看答案」无反应 | `wireDetail` 补绑 `[data-reveal-check]` | 实测:两个入口都能揭示/收起检查答案 |
| `Data.userDocs =` 不更新闭包,删除后目录/搜索残留 | `Data.reloadUserDocs()`;删除保存失败时报错不执行 | 实测:导入 MD→搜索命中→删除→目录与搜索即时同步 |
| PDF「保留原文件入口」失实 | 诚实降级:只记文件名+明确文案+「选择原 PDF 打开」按钮(运行时选文件、object URL 本地打开);未解析文件不入正文索引 | 代码审查+文案核对;文件选择器在无头环境不可自动测,已标注 |
| Markdown 列表吞同级项、`<br>` 被转义成字面文本 | 重写列表解析:同级各自成 `<li>`、深层缩进成嵌套 `<ul>/<ol>`、续行并入当前项、inline 转义后再拼 `<br>` | 行为测试 6 用例+真实文档渲染(20+ li、无字面 `<br>`) |
| DIV 点击无键盘入口;空格快捷键拦截按钮原生激活 | q-item/ri-main/search-item 加 tabindex+role+Enter/Space;快捷键焦点在交互控件上一律让路;学习页监听随路由清理 | 实测:Tab 聚焦 q-item→Enter 打开;焦点在按钮上按空格不再触发展开全部(真实按键在此自动化环境无法送达页面,原生激活按规范推断,已如实标注) |

## 五、内容修订(E 组,全部 11 项完成)

| 题目 | 修订 | 证据 |
|---|---|---|
| PY-003 | 「单线程内没有数据竞争」→ 协作式调度仍有跨 await 竞争;新增 100 协程计数反例+Lock 对照;口述的「我实际写过…」改为条件式模板 | 反例本机运行:无锁 10/1000,加锁 1000/1000(Python 3.13) |
| PY-022 | TaskGroup 语义精确化:非取消异常才取消兄弟;单子任务自取消不算失败;父取消传播;新增追问 | 三场景本机运行验证(Python 3.13) |
| PY-038 | 误区「未匹配类型静默丢失」→ 未匹配部分打包成组继续传播;核查升级 verified | 分区示例运行:ValueError 分支拿 [1,3],TypeError 残组上抛 |
| EN-009 | 「10K TPM÷2K=5 QPS」(60 倍错)→ 5 请求/分钟≈0.083 QPS;容量例题全部重算(50QPS×3.5K=10.5M TPM,132 账号不现实的诚实结论) | 算式复算通过(行为测试断言) |
| FD-026 | KV 显存表 1/8/32 GB → 0.5/4/16 GiB;补齐假设(batch=1、32 层、8KV 头、128dim、FP16);检查题同步 | 三档复算通过(行为测试断言) |
| LP-003 | strict 示例改为官方 SDK `parse()` helper(并说明 model_json_schema 缺 additionalProperties:false 的实测原因);拒答/截断/schema/API/业务校验四类失败分开处理 | pydantic 2.10.4 实测 schema 缺 additionalProperties;API 调用未真实发生(已标注) |
| RG-070 | 「证据占 75% 时换模型只能省 25%」误导 → 证据按生成模型输入单价计费,换模型影响输入+输出全部费用;删除「无损」承诺,以评测为准 | 逻辑修正,检查题/大白话同步 |
| FD-007 | FlashAttention-2 链接冒充 GQA 出处 → 拆成三条论文各附正确 arXiv(2205.14135/2307.08691/2305.13245) | arXiv 编号逐一核对 |
| LP-033 | 「manyOf」(不存在)→ anyOf/oneOf 及平台支持差异;95% 成功率改为示意值标注 | JSON Schema 事实核对 |
| PY-004 | 「类型→约束→自定义」→ before→核心→after(核心失败 after 不执行),wrap 包裹 | pydantic 2.10.4 运行验证执行顺序 |
| python-backend.md | 「元组可哈希」→ 元素全部可哈希才可((1,2) vs ([1],2)) | 本机 TypeError 实测 |

附加内容维护:
- RG-064 语义去重:CI 门禁要点并入 RG-055;RG-064 改写为「上线后检索质量漂移监控/nightly」新情境;旧 ID 与用户记录原样保留。映射见《RG-055-RG-064合并与迁移映射.json》
- AG-001/AG-002 互相前置成环修复(AG-001 无前置,AG-002 前置 AG-001);全库扫描 0 成环
- 捏造经历清零(全库 interview 字段扫描);「高频/必考/真题」类断言清理 3 处(其余「高频」均为技术语境:词频/任务频次)
- 核查诚实化:184 题 verified→partial(无可打开来源 URL,note 注明原因与日期);现分布 verified 54 / partial 252 / todo 0;《内容核查记录.md》按实际重写
- 章节反链:6 个专题主文档新增「基础必学练习组」(共 52 道基础题);备考路线文档新增 52 题基础必学清单

## 六、统计(2026-09-07,构建产物实测)

- 正式题目:306(基础 52 / 进阶 164 / 高级 90)
- 核查状态:verified 54 / partial 252 / todo 0(184 题为本次诚实降级,原因已写进每题 verify.note)
- 结构完整题:306(validate_bank 0 错误 0 警告)
- 实际内容核查题:54(全部有可打开来源 URL);另有 8 题本机运行验证(含在上者)
- 来源缺口:252 题 partial(其中 184 题待补具体来源页)
- 已运行代码例子:协程竞争/TaskGroup 三场景/except* 分区/Pydantic 顺序/元组哈希/KV 显存三档/TPM 换算(7 组)
- 语义合并:1 组(RG-055/RG-064),旧 ID 保留,映射文件已交付

## 七、功能测试记录

见《功能测试记录.md》(逐条:环境/操作/预期/实测/结论,含未验证项的如实标注)。

## 八、备份与恢复说明

- 备份入口:维护页 →「个人记录备份」
  - 导出个人记录(aiiv-records):状态/收藏/笔记/自测轮次/未完成草稿/UI 状态
  - 导出题库与资料(aiiv-library):你导入的题目 + 导入的文档
  - 导出完整备份(aiiv-full):以上全部
- 恢复:对应「导入记录 / 导入题库与资料」按钮,自动识别类型;整体校验通过才原子写入,坏文件不会半导入;重复导入幂等(轮次按内容 ID 去重)
- 已验证:导出→清空→导入恢复(收藏/笔记原文回来);重复导入不产生重复轮次;行为测试覆盖迁移规则(旧备份只补空、不覆盖新笔记)
- 未验证:超大备份(接近 localStorage 5MB 配额)的失败路径仅有代码层防护与报错文案,未用真实大文件实测

## 九、未完成事项(如实列出)

1. 252 题 partial 的来源补齐与逐条核对(184 题为诚实降级,需分批补具体来源页;基础题优先)
2. 进阶/高级题的章节练习组未建(基础 52 题已入组)
3. LP-003 代码示例未真实调用付费 API(SDK helper 写法以所装版本为准)
4. PWA 离线能力此前在 Pages 线上验证过(2026-09-06,commit 0498044);本轮修改后未重新部署验证
5. 「选择原 PDF 打开」的文件选择器无法在无头环境自动化验证(代码路径已审查)
6. 浏览页批量操作的键盘可达性未专门处理(鼠标路径正常)
