# 修改说明(第三轮:数据保护收尾、教学证据、路径闭环)

- 日期:2026-09-07 · 基线:已验收提交 `2be1168`(工作树改动 **未提交未推送**,等你验收)
- 验证环境:Windows 10 / Chrome(1265×720 + iframe 390×844)/ 隔离源 127.0.0.1:8766 / Python 3.13.9 / pydantic 2.10.4 / openai 2.34.0(仅异常类,无网络) / Node 24
- 你的真实数据(8765)全程未动,回归确认完好(306 记录/1 轮/笔记)
- 测试总量:Node 行为测试 74 项、LP-003 mock 18 项、路径练习 13 项,全部通过

## 一、数据保护(修复 1–3)

### 1. 隔离保存失败保留原始字节
- `quarantineAdd` 现在返回明确结果:true=原文已安全入隔离区;false=写入失败。**只有全部坏条目确认安全入区后,才允许用合法子集重写原库;任何失败 → 原键字节原样保留**,内存仍跳过坏条目(站点可用),`loadIssues.quarantineFailed` 记录失败数(每次 Data.init 重置,不跨轮累积)。
- 隔离条目按 kind+raw 内容幂等去重——重复启动不累积相同项。
- 新增 `Store.rawExtrasExport()`:直接导出原键字节;维护页在隔离失败时显示「导出原始内容 + 重试」双按钮,失败提示如实说明"坏数据保留在原位未被修改",不声称已回滚。
- Node 断言(6 项,已入 behavior-tests):容量失败时原键字节一致、隔离导出为空(无假成功)、原始导出含坏题原文、重试后隔离成功且原库归一、重复启动计数不增;坏 JSON 资料同路径验证。正常隔离与 full 恢复第三键回滚未改动(上轮已过)。

### 2. 共用 schema 兼容真实题库
- 先盘点事实:真实题库 7 种来源 kind(independent 268 / official 54 / official-docs 36 / paper 29 / repo 22 / official-blog 4 / website 3)。
- 枚举扩展为全集(`official-docs/official-blog/website` 入列);`website` 提供归一映射 `normalizeSourceKind('website')==='web'`,UI 标签(verifyBlock)补全 8 种中文名。**43 题内置数据未删除、来源名称/URL 原样保留。**
- Node 断言(3 项):用真实 kind 构造的 7 题全部通过资料导入;历史扩展库(全 official-docs)启动零隔离;`tags:"Python"` 坏结构仍被整批拒绝。`tools/validate_bank.py` 从未校验 kind(Python 侧与前端规则一致性核对过),无需改。
- 实测:AG-001(official-blog)/AG-004(official-docs)等真实来源在站点正常显示中文标签。

### 3. 备份恢复包含阶段确认与阅读位置
- 备份 schema 扩展并校验:`ui.pathProgress`(对象:stageId → {done, cancelled})、`ui.docPos`、`ui.pathVersion`。
- **取消语义可追溯**:标记完成写 `done: ts`;取消不再删除条目,写 `cancelled: ts`——done 记录保留,可追溯。
- 合并规则(`mergePathProgress`,个人记录与 full 恢复共用):按阶段比较最新事件(done vs cancelled),**晚者胜;同刻本地胜**;旧版 `stageId: number` 形状自动迁移为 `{done: n}`;docPos 单槽——本地为空才采用备份(阅读位置是设备本地最近状态),本地有则保留;pathVersion 本地为空才采用。**不再盲目 Object.assign 覆盖较新进度。**
- Node 断言(7 项):取消晚于备份→保留取消态;备份取消更晚→采用;本地为空→采用;旧形状迁移;docPos 两个方向;full 恢复同样生效。
- 浏览器实测(真·全新会话):造出阶段确认+docPos+笔记+收藏+轮次的 full → **逐键清空所有 aiiv:* 后整页 reload**(比"清 localStorage 刷新"更彻底,且验证了清空后内存确实为空)→ 走「导入完整备份」→ 阶段 done/docPos(y=1056,secId=6)/笔记/收藏/轮次/扩展题/资料**全部一次恢复**;重复恢复幂等。

## 二、教学解释与测试证据(修复 4–7)

### 4. FD-027:不按名称推断
- 答案/原理/口述/追问/误区/例子/检查全部重写为准确边界:**Base=预训练基座(续写行为由训练目标决定,可推断);Instruct/Chat 是提供方产品命名,可重叠、非互斥训练阶段**。
- 两个一手反例直接写进例子:InstructGPT(SFT→RLHF,名字仍 Instruct,arXiv 2203.02155)、Meta Llama-3.1-Instruct(官方多轮对话模型,模型卡)。新增追问「名字带 Instruct 是否一定单轮」与「chat template 是什么」。
- 验证方式:学生按检查题可回答"读模型卡三件事"(训练流程/多轮支持/chat template)。"名字带 Instruct"的来源已更正为 official-docs(Llama 模型卡)。

### 5. LP-003:三层区分
- 运行验证(本机 pydantic 2.10.4):`amount: float` 时 -3 通过;`Field(gt=0)` 后结构层拒绝,产物含 `exclusiveMinimum: 0`。
- 展示代码重写:`amount` 改 `Field(gt=0)`(数值约束进 schema);业务分支改为**订单归属当前用户**(真正依赖外部状态,应用层查询——示意规则查库);原理/误区/检查题同步为三层表述:①schema 能表达数值约束(exclusiveMinimum/gt=0);②平台子集限制≠schema 表达力缺陷(查平台文档);③外部状态规则必须应用层。
- 「原样重试必然同样结果」的绝对表述已移除,改为工程策略表述(无信息增量才不重试)。

### 6. mock 真正命中业务分支 + CI
- 旧测试确实存在报告指出的问题:测试 Order 有 `gt=0`,负金额在构造 mock 时就抛 ValidationError,业务分支 0 次命中。
- 重写后(18 项断言,全部从题库提取展示代码):新增**数值约束路径**(gt=-3 违反在结构层被拒,进 ValidationError 分支,回喂含 greater_than 语义)与**业务规则路径**(schema 完全合法的 `u_99-1` 订单对象 → 应用层归属校验拒绝 → 终态消息含「不属于当前用户」→ 重试至上限 3 次 → 回喂错误,首请求无回喂证明错误由应用层产生)。拒答 1 次终止、截断专属分支、临时错误 3 次成功、schema 回喂、合法返回各分支均有实证。
- CI:`.github/workflows/validate.yml` 新增两步——LP-003 mock(安装 openai/pydantic)与 `tests/path_exercise_test.py`,Python 测试不再缺席。

### 7. 无测量断言修净 + 来源补证
- LP-033 直接答案的「嵌套是质量与延迟的敌人」改为条件化(程度取决于模型/任务/schema,以评测为准);全库扫描『双杀/显著/必然/必定/保证了』仅剩 LP-001 的反例引文(教学用错误说法引用,非断言)。
- 14 道无 URL 基础题:5 道补具体官方文档(OpenAI structured-outputs/refusals、JSON Schema、Google ML、RAGAS);9 道纯实践主题保持 partial 且**待核查项写具体**(如"与 MGSM 基准对照""与 ADR/术语库实践对照"),不填首页凑数。
- 现状:verified 62 / partial 244;无 URL 170;基础无 URL **9/52**。统计口径已在核查记录声明:结构统计≠62 题全部事实的独立认证。

## 三、路径闭环(修复 8–10)

### 8. 阶段 6 完整工具调用循环
- 原片段保留为"单次派发"已删,改为完整 fake-model 闭环:用户输入→模型意图→应用执行→**工具结果以 role='tool' 回传**→模型下一轮决策→final;带 max_rounds 护栏、未知工具回喂、工具异常包装。
- 自动化验证(`tests/path_exercise_test.py`,13 项):**从 paths.json 提取代码运行**,天气问题模型调用 2 次(第二次请求含工具结果——断言输出含「根据工具结果(…晴 25°C…)」)、无需工具的问题 1 轮直接回答、TOOLS 清空后未知工具回喂且按规则结束(错误被明示,不冒充数据)、工具异常被包装(不透传)、把模型改成永远要工具时护栏触发「达到最大轮次」。每步由谁执行(模型点名/应用执行)在变式参考答案中展开。

### 9. 变式参考答案结构化
- paths.json 全部 6 阶段变式改为 `{question, reference, reason, run}` 结构:问题可见,参考答案/原因默认折叠。
- **阶段 2 预设错误已修**:不再问"为什么必须 range 而不是 while len"(while 推进索引同样正确),改为对比两种终止条件的写法、size<=0 快速失败、故意不推进索引的反例;测试同时验证 range/while 两版输出一致、忘记推进确实死循环(限时超时捕获)。
- PathView 揭示界面:「变式检查」问题区 + 「展开参考答案(先自己回答)」折叠区 + 原因/反例子区。看过答案不自动标掌握;阶段确认仍手动。390px 下 6 阶段/变式/长代码全部正常。
- 验收走查(浏览器):阶段 1→问题→展开参考→运行说明→取消确认留痕(上一轮机制)均在;中断恢复与备份恢复见修复 3 的新会话实测。

### 10. path-q 键盘修复
- 整行从 `div[role=link][tabindex]` 改为**真 `<a href>`**:单一焦点停靠点(内层不再有嵌套链接),Enter/Space 走浏览器原生链接激活,无 JS 合成路径。实测:href 正确指向 `#/study/PY-001`、`isAnchor=true`、内层 a 数量为 0。真实按键在无头环境无法送达页面(Enter 事件未能注入),此为环境限制——语义与结构按标准可达性实现,如实标注。

## 验证汇总
- 行为测试 74/74(新增隔离失败保原文 6 项、来源兼容 3 项、pathProgress/docPos 合并 4 项等 13 项)
- LP-003 mock 18/18(业务分支真实命中)、路径练习 13/13(展示代码运行+反例)
- validate_bank 0 错 0 警、dedup 无预警、build 可复现、node --check 9 个 JS 全过
- 浏览器:隔离提示/原始导出按钮、全新会话一次完整恢复(7 类数据+幂等)、变式揭示、path-q 真链接、390px、8765 真实数据完好

## 未完成 / 未验证(如实)
- 244 题 partial 的来源补证继续(基础题剩 9 道纯实践主题,待核查项已具体化)
- 真实手机、跨浏览器、PWA 升级路径、PDF 文件选择器、配额失败的真实复现(容量失败用注入异常模拟,未用真实大文件)
- LP-003 mock 为桩客户端,非真实 API 调用;openai/pydantic 版本固定在 CI 中用宽松范围(>=2.0 / >=2.5),行为漂移可能需跟进
- 多路径支持未建(单主线);content_version 只覆盖前两轮修订题
