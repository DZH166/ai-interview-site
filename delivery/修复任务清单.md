# 修复任务清单(基于审查版本 0498044)

- 基线:`git rev-parse HEAD` = `0498044cfce2d56d444cdfe1f043662dd701a898`,工作树干净,无更新提交,报告问题全部适用。
- 约束:不提交/不推送/不清用户数据;题号保留;合并去重留迁移映射;本地可用。

## A. 复习与历史
- [x] A1 `MockView.startTodayReview` 不存在(views-review.js:143)→ 实现 `MockView.startDirected(qids,label)` 定向复习,复用自测会话与草稿持久化
- [x] A2 `renderRounds` 未定义(views-review.js:64)→ 实现模拟面试历史:轮次列表+详情(题目/我的回答/是否看过参考/复盘/时间),兼容旧格式
- [x] A3 今日队列文案「做完自动消失」与实际不符 → 改为明确规则:标「基本掌握」移出;标「还不熟/待复习」保留;不自动假定掌握

## B. 检索/定位/目录/继续阅读
- [x] B1 search.js:39,50 章节 unit `raw=sec.title` → raw 改为标题+正文,片段显示真实上下文与高亮
- [x] B2 views-knowledge.js:296 题目/笔记结果丢 `u.anchor` → `#/study/<id>?a=<field>`,StudyView 按需展开并滚动到对应区块(check 同时揭示答案)
- [x] B3 锚点协议:TOC 传 `sec-N`,jumpToSection 再拼 `sec-` → 统一归一化(剥离 `sec-` 前缀后拼接)
- [x] B4 `#view.onscroll` 无效(实际 window 滚动)→ 全部改 window 滚动:进度条、位置恢复、锚点滚动、currentSection
- [x] B5 验收场景:中文/英文大小写/代码文本/多关键词/文末命中/笔记命中/无结果;返回保留关键词与位置

## C. 导入校验与记录保护
- [x] C1 validateQuestions:同批 seen 未记录、fresh 不看 errors、extraBankSave 返回被忽略 → 完整 schema 校验(类型/枚举/嵌套/同批与库存冲突/乱码/引用/来源格式),仅导入 0 错题,保存失败不提示成功
- [x] C2 自测草稿仅在内存 → draft 落盘(config/items/idx/answers),配置页可恢复继续,完成/放弃清除
- [x] C3 store.importRecords:类型/版本校验、笔记覆盖规则(按记录级 _updatedAt,旧备份不覆盖新笔记)、轮次稳定 ID 幂等、原子写入失败即报错不半导入、保留空串与 false 语义
- [x] C4 导出分范围:个人记录 / 导入题库+资料 / 完整备份;导入自动识别类型;坏类型明确报错
- [x] C5 验收:非法 JSON 新增 0 题;合法题保存且刷新存在;同备份重复导入幂等;隔离环境恢复

## D. 交互断点
- [x] D1 `#/browse?t=xxx` 未消费 → BrowseView 读 query 并应用筛选(Python=44 题),replaceState 清理 URL
- [x] D2 relLinks `doc-${topic}-1` 失效 → 按真实 docs(topic 相同、order 最小)映射;无文档则隐藏行
- [x] D3 app.js:18 study 清空 NavCtx → 移除;neighbors 对不在集合的题回退全量
- [x] D4 「全部状态」与「未练习」共用空值 → 新增 `__none` 选项,apply 支持
- [x] D5 BrowseView.wireDetail 未绑 `[data-reveal-check]` → 补绑定
- [x] D6 `Data.userDocs =` 不更新闭包 → Data 提供 reloadUserDocs(),删除后同步目录/索引
- [x] D7 PDF「保留原文件入口」失实 → 降级为诚实提示(仅记住名称)+「选择原文件打开」按钮(运行时选文件、object URL 打开);未解析文件不入正文索引
- [x] D8 markdown.js 列表吞同级项 + `<br>` 被转义 → 重写列表解析(有序/无序/嵌套/续行),转义顺序修正
- [x] D9 键盘:q-item/search-item 加 tabindex+role+Enter/Space;快捷键排除交互控件;space 不再拦截按钮原生激活;路由切换清理监听

## E. 内容正确性与核查诚实性
- [x] E1 表格 11 题:PY-003 协程竞争反例 / PY-022 TaskGroup 取消语义 / PY-038 except* 传播 / EN-009 TPM-QPS 重算 / FD-026 KV 缓存算式(0.5/4/16 GiB) / LP-003 strict schema+refusal / RG-070 证据计费 / python-backend.md 元组可哈希 / FD-007 论文链接拆分 / LP-033 manyOf / PY-004 validator 顺序
- [x] E2 口述真实性:「我实际写过…」等改为条件式模板(排查 interview 字段)
- [x] E3 RG-055/RG-064 语义合并,保留旧 ID 迁移映射
- [x] E4 前置关系成环(AG-001/AG-002)修正;基础必学路径明确
- [x] E5 核查状态诚实化:186 题 verified 无非空来源 URL → 降级 partial(注明待核查),有真实 URL 的保留;核查记录文档按实际重写
- [x] E6 无证据的「公司真题/必考/高频」类表述排查

## F. 验证与交付
- [x] F1 `node --check` 全部 app/js/*.js + tools 相关
- [x] F2 行为测试(Node 存根):markdown 列表、搜索片段与锚点、Store 导入合并/幂等、validateQuestions 拒绝用例、FD-026/EN-009 计算复算
- [x] F3 浏览器回归:复习启动/历史、搜索定位、导入拦截、草稿恢复、导航/检查题、键盘、390px 移动端、长代码/长题干/弹窗
- [x] F4 CI 覆盖 JS 语法检查(本地脚本先行,workflow 更新但不推送)
- [x] F5 交付:修改说明、统计(正式/待整理/结构完整/实际核查/来源缺口/可运行例子)、合并映射、修订清单、测试记录、备份恢复说明、未完成事项

## 执行记录(2026-09-07 完成)

- A 组:MockView.startDirected + renderRounds 实现,浏览器闭环实测通过(队列规则:ok 移出/weak+review 保留)
- B 组:片段/锚点/归一化/window 滚动全部实测;过程中另发现并修复 smooth-scroll 与 SW 旧缓存两个新问题
- C 组:非法用例 0 写入、混合导入只收合法题、备份三分与幂等,行为测试 24 断言 + 浏览器实测;另发现并修复 Data.init 不可重入
- D 组:9 项全部完成;D7 PDF 选择器交互无法无头自动化(代码审查),D9 真实按键无法送达(合成事件验证守卫)
- E 组:11 题全部修订且附运行证据;RG-055/064 合并(旧 ID 保留+映射文件);成环清零;捏造经历清零;184 题 verified 诚实降级
- F1~F6:全部完成(CI 工作流已加 node --check 与行为测试两步,按约定未推送)
- 明细:《修改说明.md》《功能测试记录.md》
