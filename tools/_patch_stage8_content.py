# -*- coding: utf-8 -*-
"""阶段8:为13道融合新增题实补 plain/deep/answer/pitfalls(改适配器输入,不绕哈希)。用后可删。"""
import json, io

f = 'data/imports/ai-series/new-questions.json'
nq = json.load(io.open(f, encoding='utf-8'))

ADD = {
 'LC-001': {
   'plain': '判断的方法是看组件的实现与协议:同步函数包了async只是换了个返回值;流式是否逐块输出,取决于该环节是否实现了逐块回调。面试时说清「哪一层在什么条件下才是真的异步/流式」,比背接口名更可信。',
   'deep': '\n\n### 观察方法\n用小段输入实测首块到达时间与末块差异,记录线程池配置,比阅读签名更能判断真实流式行为。',
   'pitfalls': '把框架统一入口当成底层行为保证。',
 },
 'LC-002': {
   'plain': '可控的做法是把「谁能调、调什么、结果怎么验」写成显式步骤:工具按角色过滤,参数先过schema再过业务校验,执行结果带证据回传。面试时能给出一处「框架不管、业务必须管」的具体例子,就说明真的接手了责任。',
   'pitfalls': '跳过业务校验直接执行模型给出的参数。',
 },
 'LC-003': {
   'plain': '保护副作用的标准做法是给每个外部动作一个业务幂等键:恢复重跑时先查该键是否已生效,已生效就复用结果而不是再次发送。图状态恢复的是「做到哪一步」,幂等键保证的是「重做也不重复产生后果」。',
   'pitfalls': '以为框架的检查点自带业务幂等。',
 },
 'LC-004': {
   'plain': '可靠的迁移路径是列一个行为清单:钩子的触发时机、可读写哪些状态、异常向谁传播、并发限制在哪,然后逐条在测试里对齐。清单上任何一条对不上,就说明迁移改变了运行语义,不能只改import宣布完成。',
   'pitfalls': '迁移后不跑既有行为测试就上线。',
 },
 'LC-005': {
   'plain': '评估它是否合适的办法是数一数代理帮你省了什么、藏了什么:省的是样板调用代码,藏的是请求细节与错误映射;若业务需要精细控制重试、租户或审计,藏在代理后面的细节就要显式拆出来。',
   'deep': '\n\n### 观察方法\n用最小Spring/Quarkus示例分别验证流式、超时与租户字段的行为,再决定是否引入;版本升级后重跑这组最小示例。',
   'pitfalls': '以为接口代理自动处理模型超时与降级。',
 },
 'LC-006': {
   'plain': '可检查的做法是给报告配证据表:每条结论对应来源、原文摘录与置信度,反例材料单独一列而不是删掉。摘要段落保留原始链接,读者能沿着它回到原文核对,报告才从「像研究」变成「可复核」。',
   'pitfalls': '只保留支持预设结论的摘要。',
 },
 'RG-071': {
   'answer': '两条材料都有效时,按业务规则说明各自适用条件并分别引用,不合并成一条。',
   'plain': '裁决过程要留下记录:引用了哪份材料的哪一条、依据什么规则、谁负责最终确认。换一个人复核时能重走同样的判断,而不是重新猜。',
   'pitfalls': '把裁决写回知识库时不注明依据与条件。',
 },
 'AG-059': {
   'plain': '组合的正确顺序是:先用MCP把工具接进来并管理权限,再让Skills说明「什么任务该用哪些工具、按什么步骤」,Function Calling仍是模型提交操作申请的机制。三层各改各的版本,任何一层缺失都降级为人工确认。',
   'deep': '\n\n### 落地检查\n部署前逐项确认:技能清单来自可信来源、每个工具的授权范围独立配置、模型申请的操作在执行层复核——三处分别记录版本与变更人。',
   'pitfalls': '把技能目录当作自动安装工具的市场。',
 },
 'AG-060': {
   'plain': '可靠的远程协作要补四件事:任务状态可查询、错误语义明确、产物有校验方式、出现争议时的责任界面。Agent Card只回答第一半——对方是谁、声称会什么;剩下的要在合同与测试里落实。',
   'pitfalls': '不验证产物就采信远程结果。',
 },
 'AG-061': {
   'plain': '衡量路由好坏要看两个数:错误工具的漏召回率,和权限边界是否始终在执行层生效。路由器只是把候选从几千缩到几十,最终那次操作仍要过授权检查——这一步省不得。',
   'pitfalls': '把路由器的过滤规则当成权限系统。',
 },
 'LP-046': {
   'plain': '分界可以背成一句话:训练决定「先验倾向」,运行时决定「每一次是否放行」。评测也要分开做——离线看工具选择的准确率,在线看每次调用的权限校验与结果核对,两条曲线缺一条都说明系统没有闭环。',
   'deep': '\n\n### 责任矩阵\n把「谁负责什么」写成表:模型负责候选与参数,规则引擎负责权限与限额,业务层负责结果核对;任何一层缺失时降级策略是拒绝执行并转人工。',
   'pitfalls': '用离线榜单替代线上权限审计。',
 },
 'LP-047': {
   'plain': '取舍的判断标准是「迟到即失效的才走实时通道」:语音、共享白板走低延迟路径;确认类消息宁可重传也不能丢。很多语音产品是WebRTC管媒体、WebSocket管信令与文本,两者是搭档不是对手。',
   'pitfalls': '把传输协议选择与应用层可靠性混为一谈。',
 },
 'FD-028': {
   'plain': '工程上的做法是按自己的真实任务测有效长度:把关键证据放在不同位置跑一组对照,画出正确率随位置与总长的变化,用这条曲线而不是标称值做切分与预算决策。',
   'pitfalls': '不做位置对照就把标称窗口写进架构文档。',
 },
}

changed = 0
for q in nq:
    if q['id'] not in ADD: continue
    add = ADD[q['id']]
    if 'plain' in add: q['plain'] = q['plain'] + add['plain']
    if 'deep' in add: q['deep'] = q['deep'] + add['deep']
    if 'answer' in add: q['answer'] = q['answer'] + add['answer']
    if 'pitfalls' in add and add['pitfalls'] not in q['pitfalls']: q['pitfalls'].append(add['pitfalls'])
    changed += 1
io.open(f, 'w', encoding='utf-8', newline='').write(json.dumps(nq, ensure_ascii=False, indent=2) + '\n')
print('patched', changed, 'questions')
