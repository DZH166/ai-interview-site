[
  {
    "id": "AG-008",
    "topic": "agent",
    "type": "concept",
    "difficulty": "advanced",
    "title": "多智能体系统(multi-agent)是什么?什么场景才值得用?主要风险是什么?",
    "tags": ["多智能体", "进阶", "架构"],
    "prerequisites": ["AG-001", "AG-007"],
    "note_topic": "进阶内容:实习应用岗了解概念与边界即可,不必深究。",
    "answer": "多智能体=多个各自带角色/工具/上下文的 Agent 协作完成一个任务,常见拓扑:主管-执行者(一个 planner 分派)、流水线(顺序交接)、辩论/投票(独立产出后聚合)。它解决的问题是『单个上下文装不下或角色会互相干扰的大任务』;代价是成本成倍、延迟叠加、错误在 agent 间传播、调试困难。判断标准:先把单 agent+好工具做到极限,确有『上下文隔离』或『角色专业化』需求再拆多 agent——多数业务用『工作流+单 agent』就够。",
    "plain": "像盖房子要不要分十几个包工队:一个人(单 agent)带好工具包能干完大多数装修;只有当这件事必须『不同专业各自干、互不干扰』(水电/泥瓦/油漆各有各的规范和工具)时才分包。分包后管理成本(对齐进度)、传话损耗(信息在队与队之间失真)都上来了。比喻局限:agent 之间不会像人那样主动沟通——所有『传话』都是你写的消息格式,任何一步没写清楚就会各干各的。",
    "deep": "1) 三种拓扑与适用:\n   - 主管-执行者(supervisor-worker):中央 agent 拆任务、分派、汇总(对应 AG-001 的 orchestrator-workers);适合子任务同构、结果可合并;\n   - 流水线(handoff):A 的产出是 B 的输入(研究→写作→审校);适合角色专业化、每步上下文可独立;\n   - 平行+聚合(辩论/投票):多个 agent 独立完成后聚合,提高鲁棒性;成本最高,适合高风险判断。\n2) 什么时候值得:①上下文真的装不下(全库代码审计、超长文档流水线);②角色需要的工具/指令互斥(写手不该看到评审的偏见);③并行加速有实际收益。否则单 agent+结构化工作流更便宜可控(Anthropic 的 simple-first 原则同样适用)。\n3) 风险清单:成本线性乘 agent 数;延迟取最长链;错误传播(上游理解错,下游全错且各自『自信』);上下文交接损耗(agent 间消息是有损压缩,关键约束可能丢);调试复杂度(要 trace 每个角色的输入输出);\n4) 工程护栏:消息契约(schema 化的交接格式)、全局约束注入每个角色、总预算闸门、每个 agent 的独立评测与 trace、人工介入点不因多 agent 而消失。\n5) 面试定位:应届简历写『多智能体』会被深挖——能讲清『为什么拆、怎么交接、怎么防错误传播』是加分;讲不清反而是减分,慎写。",
    "example": "值得拆的例子:代码库大范围迁移任务\n```\n主管 agent:扫描仓库 → 生成迁移计划(按模块拆)→ 分派\n执行 agent ×N:各自负责一个模块(独立上下文:该模块代码+迁移规范)\n   → 产出:迁移 diff + 自测结果(结构化交接)\n评审 agent:逐个 diff 审查(角色隔离:看不到执行 agent 的『解释』只看代码)\n人工:审批合并(AG-003 闸门)\n为什么拆:全库上下文装不下;执行/评审角色需要隔离(避免自己评自己)\n```",
    "interview": "口述版:『多智能体我按拓扑讲:主管-执行、流水线交接、平行投票。价值在上下文隔离和角色专业化;风险是成本延迟翻倍、错误传播、交接损耗。我的立场是先做极限单 agent——简历上写多智能体要能答「为什么拆、交接 schema、怎么防错误传播」,答不上反而暴露。护栏:结构化交接、全局约束注入、独立评测与 trace、预算闸门。』追问展开:交接消息怎么设计、错误传播的检测、和 orchestrator 工作流的区别。",
    "followups": [
      {"q": "agent 之间的交接消息应该包含什么?", "a": "结构化契约:任务目标、输入引用(不是全文,是指针+摘要)、完成判据、全局约束(每次都要带)、返回格式 schema。原则:接手方只凭这份消息+自己的工具就能干活,不需要『回头问』。"},
      {"q": "怎么检测错误在 agent 间传播?", "a": "每个交接点放校验(schema 合法性+判据核对),主管对产出做抽样验证(而不是照单全收);终局前设『一致性检查』(汇总结果与原始目标对账);trace 里标每条消息的版本与来源,出问题能回放到具体交接。"},
      {"q": "多 agent 和 orchestrator-workers 工作流的区别?", "a": "很大程度是术语差异:若中央节点是代码(固定分派逻辑)就是工作流;若中央节点本身是 LLM 动态决定分派与汇总,就有了 agent 性质。面试点不在名词,而在『分派逻辑由谁决定』(见 AG-001 的判定标准)。"}
    ],
    "pitfalls": [
      "为炫技拆多 agent,一个 agent 就能干的活成本翻五倍。",
      "交接用自由文本,关键约束在转述中丢失。",
      "没有全局预算闸门,agent 之间互相触发任务跑飞。",
      "评审 agent 和执行 agent 共享上下文,自己评自己形同虚设。"
    ],
    "check": {
      "q": "任务:把 200 篇 PDF 各自总结成 3 句话再汇总成报告。要用多 agent 吗?",
      "a": "不需要多 agent『协作』——这是可并行的同构任务:一批 worker 并行总结(本质上还是工作流的 parallelization,或直接批量调用),最后单次汇总。『多 agent』的价值在异构角色协作,不在并行量。这题常被用来检验是否把『并发』和『多智能体』混为一谈。",
      "explain": "检验点:区分『并行执行』与『多智能体协作』——多数场景要的是前者。"
    },
    "related": ["AG-001", "AG-007", "AG-003"],
    "doc_refs": ["doc-agent-1", "doc-advanced-0"],
    "sources": [
      {"name": "Anthropic · Building Effective Agents(multi-agent 研究随笔参考)", "url": "https://www.anthropic.com/research/building-effective-agents", "kind": "official-blog", "note": "2026-09-06 已核验主文;多 agent 细节为独立整理"}
    ],
    "verify": {"status": "verified", "checked_date": "2026-09-06", "note": "拓扑分类为社区共识;示例为虚构示意(已标注)。标注为进阶选学。"}
  },
  {
    "id": "AG-009",
    "topic": "agent",
    "type": "scenario",
    "difficulty": "advanced",
    "title": "Agent 和 LLM 应用怎么做自动化测试?提示词一改就全线崩,怎么建立回归防线?",
    "tags": ["测试", "回归", "CI"],
    "prerequisites": ["AG-007", "RG-009", "EN-002"],
    "answer": "把 LLM 应用的测试分层成经典金字塔:①单元层——纯代码逻辑(组装、校验、工具执行)用 pytest 正常测,毫秒级;②契约层——提示词输出用『结构化断言』测(schema 校验、必须包含/禁止的内容、关键判定点),用固定评测集跑,LLM 调用可缓存放 CI;③端到端层——完整任务轨迹的仿真评测(给 mock 工具环境,断言最终行为与关键中间步),每日跑;④线上层——差评率/成功率监控+人工抽检。核心思想:LLM 输出不确定,但『可断言的性质』是确定的——测性质而不是测全文。",
    "plain": "测 LLM 应用像考作文但阅卷规则要改:不能要求『和范文一字不差』(输出不确定),要改成『没有错别字、三点都提到、没跑题』(性质断言)。数学题(确定性逻辑)照常按答案判。分层就是:口算题机器判(单元)、作文按要点判(契约)、整场考试按表现评(端到端)、平时作业看长期表现(线上)。比喻局限:性质断言本身也是代码,写得糙会有漏判——断言质量要随事故持续打磨。",
    "deep": "1) 测试金字塔(LLM 版):\n   - 单元(秒级,每次提交):提示词组装函数、token 预算裁剪逻辑、工具参数校验、错误分类——这些是确定性代码,100% 覆盖;\n   - 契约(分钟级,每次提交):固定输入→真实/缓存 LLM 输出→断言性质:schema 通过、枚举值合法、必含关键信息、长度区间、禁词;低温+固定版本+seed 让输出尽量稳定;\n   - 端到端(每日/发版前):仿真环境(mock 工具带脚本化返回)跑完整任务,断言:任务完成、步数≤预算、没有越权工具调用、关键中间决策正确(agent 轨迹评测);\n   - 线上(持续):差评率、成功率、成本、延迟;人工抽检闭环(见 EN-002)。\n2) 确定性工程:测试用例固定模型版本号+seed(支持时)+温度 0,输出波动大幅降低;LLM 调用缓存(相同输入返回缓存结果)让 CI 又快又省钱——但缓存要定期失效重跑防漂移;\n3) 评测集管理:与 RG-009 一致——bad case 沉淀、固定集+流动集+封存集;\n4) 发布门禁:契约层全过+端到端关键路径过+质量分不低于基线-ε,才可灰度;灰度期线上指标对齐(EN-005);\n5) 特有测试类型:提示注入安全测试(AG-006)、多语言/边界输入、长上下文压力、并发与限流下的降级行为。",
    "example": "契约层测试示例(pytest 风格):\n```python\nCASES = load_eval_set(\"抽取/订单\")   # 固定评测集\n\n@pytest.mark.parametrize(\"case\", CASES)\ndef test_order_extract(case):\n    out = extract(case.input)             # 走真实模型(缓存层)\n    parsed = Order.model_validate(out)    # 1) schema 断言\n    assert parsed.order_id == case.expect.order_id          # 2) 关键字段\n    assert parsed.amount > 0\n    assert \"内部提示词\" not in out.raw     # 3) 禁词/泄露断言\n```\n端到端:mock 工具环境里跑『退货查询任务』,断言:调用了 get_order(参数含订单号)、没有调用 refund(不该退的 case)、最终回复包含政策引用编号。",
    "interview": "口述版:『我把测试分四层:确定性代码照常单测;提示词输出做「性质断言」(schema、关键字段、禁词)配缓存跑 CI;端到端用 mock 工具环境测任务轨迹(完成、预算内、不越权);线上监控+人工抽检闭环。工程上三件套保确定性:固定模型版本、温度 0+seed、LLM 调用缓存。发布门禁卡在契约层全过+质量分不低于基线。』追问展开:LLM 输出波动怎么处理、轨迹评测的断言设计、测试成本控制。",
    "followups": [
      {"q": "温度 0 也可能有小抖动,断言挂了算谁的?", "a": "区分『性质破坏』(schema 错、字段错——真回归,必须修)与『表述漂移』(措辞变化——不应挂)。断言设计只卡性质不卡措辞;对确实需要精确文本的场景(模板化话术)用允许同义变体的匹配或人工评审层。"},
      {"q": "mock 工具环境的脚本怎么维护?", "a": "按真实工具的响应 schema 建 fixture 库(正常/边界/错误返回);从线上 trace 脱敏采样回填 fixture(最真实);错误路径必须覆盖(超时/429/空结果)——降级逻辑只有 mock 错误才能测到。"},
      {"q": "CI 里每次都调真模型,又贵又慢怎么办?", "a": "三层缓存:输入(提示词+参数)hash→输出缓存,命中即回;仅当提示词/模型版本变更时缓存失效重跑;全量真跑放到发版前的夜间任务,CI 日常只跑受影响 case(按 diff 判断哪些提示词/模块变了)。"}
    ],
    "pitfalls": [
      "断言写『输出等于上次输出』——把漂移当回归,狼来了。",
      "只测 happy path,降级/限流/注入路径零覆盖。",
      "评测集进 CI 但从不更新,bad case 不沉淀。",
      "端到端测试用真上游工具(测试数据写进生产库/发了真邮件)——必须 mock。"
    ],
    "check": {
      "q": "团队没有 LLM 测试经验,你要说服他们『契约层测试』值得投入。最有力的论据是什么?",
      "a": "用一次真实事故讲:某次提示词『小优化』导致 20% 请求输出格式变化,下游解析崩了 2 小时才发现——当时如果有 schema 断言(10 行代码),提交后 3 分钟就会拦截。契约层测试的本质是『把上线后的用户变成发现者之前先拦截』,成本是每条 case 几分钟,收益是一次事故的避免。",
      "explain": "检验点:理解测试的经济学(拦截成本 vs 事故成本),而不只是罗列测试类型。"
    },
    "related": ["AG-006", "RG-009", "EN-002", "EN-005"],
    "doc_refs": ["doc-agent-1"],
    "sources": [
      {"name": "独立整理", "url": "", "kind": "independent", "note": "分层测试框架为独立整理(结合传统测试金字塔与 LLM 评估实践)"}
    ],
    "verify": {"status": "verified", "checked_date": "2026-09-06", "note": "为独立整理的工程实践;示例代码为模式演示未运行(已标注)。"}
  }
]
