[
 {
  "id": "LP-001",
  "topic": "llm-prompt",
  "type": "concept",
  "difficulty": "basic",
  "title": "temperature 和 top_p 到底在控制什么?什么任务该调高、什么任务该调低?",
  "tags": [
   "LLM API",
   "采样参数",
   "temperature",
   "top_p",
   "AI系列融合"
  ],
  "prerequisites": [
   "FD-001"
  ],
  "answer": "temperature通常缩放logits后再归一化，Top-K保留固定数量候选，Top-P按累计概率选候选集。接口可能同时支持它们，具体推荐依模型和任务而异，不存在统一最佳组合。T=0或固定seed也不能无条件保证跨硬件、版本和服务实现逐位可复现。",
  "plain": "先改变候选分布，再选择考虑的候选集合，和真正保证事实正确是不同问题。降低随机性可能改变一致性，但不会自动修复错误知识或任务理解。",
  "deep": "### 参数作用\n区分概率重分布与截断；不要把temperature理解成答案正确率旋钮。\n\n### 实现约定\n模型可能推荐组合配置，也可能有特殊解码行为；运行时记录实际接受的参数与版本。\n\n### 评估与复现\n固定输入、模型、硬件和采样配置后记录重复运行分布；数学和代码任务也应看模型说明与任务评测，而非一律设0。\n\n### 可验证的对照\n实验记录应包含模型与服务版本、实际生效参数、输入、输出长度和重复次数。若供应商忽略或不支持某参数，配置文件里写了它也不能当控制变量。比较结果既看质量分布，也看格式、错误拒答和成本；对同一道题重复采样的稳定性，不等于对新题的可靠性。把参数当作候选实验配置，比给所有数学、代码或创作任务规定一个万能值更有解释力。",
  "example": "对照实验设计：固定一组可判定的提取和开放写作任务，比较几组接口支持的配置，记录格式通过率、质量与重复运行差异。不预填“最佳参数”，也不把同一次成功当可复现证明。",
  "interview": "temperature通常缩放logits后再归一化，Top-K保留固定数量候选，Top-P按累计概率选候选集。接口可能同时支持它们，具体推荐依模型和任务而异，不存在统一最佳组合。T=0或固定seed也不能无条件保证跨硬件、版本和服务实现逐位可复现。\n\n深入展开：参数作用；实现约定；评估与复现。用下面的设计例子说明，不能当成用户已实现的项目经历。",
  "followups": [
   {
    "q": "Top-P与Top-K不能一起用吗？",
    "a": "可以在支持的实现中组合，具体顺序与参数语义按接口核对。"
   },
   {
    "q": "T=0就没有幻觉吗？",
    "a": "没有这样的保证，解码随机性与事实支持是不同问题。"
   }
  ],
  "pitfalls": [
   "所有推理任务统一T=0。",
   "固定seed保证任意设备和版本一致。"
  ],
  "check": {
   "q": "同样T=0两次输出不同一定是业务代码错吗？",
   "a": "不一定，服务、硬件、内核及版本也影响复现；应记录环境并按实现保证排查。",
   "explain": "检验重点：所有推理任务统一T=0。；固定seed保证任意设备和版本一致。这些是需要主动排除的误解，应结合本题的条件说明判断依据。"
  },
  "related": [
   "LP-002",
   "LP-003",
   "FD-002"
  ],
  "doc_refs": [
   "doc-llm-1",
   "doc-ai-series-1"
  ],
  "sources": [
   {
    "kind": "official-docs",
    "name": "huggingface.co · Qwen3-8B",
    "url": "https://huggingface.co/Qwen/Qwen3-8B",
    "note": "本轮融合的一手依据；按所引版本及实验范围理解，未据此声明用户已完成项目。"
   },
   {
    "kind": "official-docs",
    "name": "docs.vllm.ai · reproducibility",
    "url": "https://docs.vllm.ai/en/latest/usage/reproducibility/",
    "note": "本轮融合的一手依据；按所引版本及实验范围理解，未据此声明用户已完成项目。"
   },
   {
    "kind": "web",
    "name": "小林系列 · 采样配置",
    "url": "https://xiaolinnote.com/ai/llm/temperature_top_p_top_k.html",
    "note": "选题对照；站内答案独立整理，网站中的经历、比例与断言不自动视为事实。"
   }
  ],
  "verify": {
   "status": "partial",
   "checked_date": "2026-09-13",
   "note": "本轮对照一手资料修订；示例为设计或算术，未运行真实模型/生产系统。事实、版本边界与工程建议分开理解。"
  },
  "fusion_notes": "### 采样配置\n\n温度、Top-P和Top-K可联合存在，具体支持和推荐依模型而异；不存在所有任务统一最佳设置，T=0也不保证服务逐位可复现。",
  "content_version": {
   "rev": "ai-series-20260913",
   "date": "2026-09-13",
   "summary": "跨字段校正概念、版本与实验边界",
   "changed_fields": [
    "answer",
    "plain",
    "deep",
    "example",
    "interview",
    "followups",
    "pitfalls",
    "check",
    "fusion_notes"
   ]
  },
  "metadata": {
   "ai_series": {
    "batch_id": "ai-series-20260912",
    "source_ids": [
     "XL-LLM-13"
    ],
    "before_hash": "2a410f1801fa4e7042d1178fa00727a6597a28434ccc6582b2fe0f28cda7837b",
    "output_hash": "e856525e957611eec4c6d9f48d1623b7ffd26f8a791ef45edc724f05ae4b0e5a"
   }
  }
 },
 {
  "id": "LP-002",
  "topic": "llm-prompt",
  "type": "principle",
  "difficulty": "intermediate",
  "title": "什么是上下文窗口?模型会『记住』之前的对话吗?对话历史太长怎么管理?",
  "tags": [
   "上下文窗口",
   "Token",
   "多轮对话",
   "记忆"
  ],
  "prerequisites": [
   "FD-001"
  ],
  "answer": "上下文窗口是模型一次请求能处理的全部 token 额度——系统提示、全部历史消息、检索到的资料、用户问题和待生成的回答都在里面,输入和输出共享这个窗口。模型本身完全无状态:『多轮对话的记忆』是应用在每次请求时把历史重新拼进请求实现的。窗口不够用时按成本排序做管理:截断最旧轮次、把旧内容压缩成摘要、或者把历史放外存(向量库)按需检索注入。",
  "plain": "把上下文窗口想成一块固定大小的白板:模型每一轮都要『看着白板上现有的一切』来接话。白板不是模型自带的记忆——是你每一轮重新把该写的东西抄上去。写满了只有三个办法:擦掉最旧的(截断)、把旧对话浓缩成几行摘要(压缩)、把不常用的搬进档案柜要用再取(检索式记忆)。比喻的局限:白板让人以为『写上去就一定被用上』,实际上注意力对超长内容会『看丢中间部分』,而且窗口内所有内容都按 token 计费。",
  "deep": "关键事实逐条拆:\n1) 无状态:HTTP API 每次调用独立,messages 数组由客户端维护并全量重传。所以『模型忘了我上一句』几乎都是应用层没传,而不是模型失忆。\n2) 预算分配:窗口 = system + 历史 + 检索内容 + 本轮输入 + 输出预留(max_tokens 从同一窗口里扣)。8K 窗口、历史 7.5K、max_tokens=1K → 直接超限报错或需裁剪。\n3) 计费与延迟都随 token 线性上涨,所以『历史管理』本质是成本/质量问题。\n4) 管理策略按开销递增:\n   a. 滑动窗口:保留 system + 最近 N 轮。简单可靠,但丢失早期约束(如用户第一句『我对花生过敏』)。\n   b. 摘要压缩:把旧轮次交给模型总结成要点,替换原文。注意摘要会丢数字/专名等硬信息,关键约束要白名单保留。\n   c. 检索式记忆:历史/知识存向量库,按当前问题检索相关片段注入。适合长期记忆,但引入检索失败风险。\n   d. 工具结果瘦身:大 JSON 结果只回传关键字段或摘要,防止一轮就把窗口吃满。\n5) 长上下文的『有效利用』问题:窗口大小 ≠ 有效注意力长度,超长上下文中部信息利用率下降(lost in the middle 现象,多项公开研究报道过);评估长上下文能力要用大海捞针类测试,不能只看标称窗口。\n6) 与 KV Cache 的关系:多轮对话中前缀不变,服务端缓存前缀的注意力中间结果(KV Cache)可显著降延迟降费——所以『稳定内容放前、易变内容放后』是通用优化(平台 prompt caching 机制,版本相关)。",
  "example": "客服机器人第 30 轮,messages 已 40K token,窗口 32K:\n```\n[system 客服规则(不变,放最前)]           # 保留\n[第 1~25 轮 → 交给模型压成 200 字摘要]     # 压缩\n[用户第 1 轮『花生过敏』→ 提为独立约束行]  # 白名单保留硬约束\n[第 26~30 轮原文]                          # 保留\n[本轮检索到的 3 段订单资料]                # 按需注入\n[本轮用户输入]                             # 保留\n→ 输出预留 2K\n```\n流程:先裁 → 后压 → 再检索,最后拼装,并在拼装后用 tokenizer 精确计数,超限就继续压。",
  "interview": "口述版:『上下文窗口是单次请求的 token 总额度,输入输出共享。模型无状态,多轮记忆是应用层每次重传 messages 实现的。历史超长时我会分层处理:system 和关键约束(过敏、身份)永远保留;旧轮次摘要压缩;大工具结果瘦身;需要长期记忆就上向量库检索注入。另外我会注意稳定前缀放在最前,好让服务端的 prompt cache 命中。』追问展开:窗口与参数量的区别(高频追问)、KV Cache、长上下文有效利用率怎么测。",
  "followups": [
   {
    "q": "上下文窗口和模型参数量有什么区别?(高频考点)",
    "a": "窗口是『一次能读进多少内容』的运行时额度,随请求变化,按 token 计费;参数量是训练后固化的权重规模(如 7B/70B),决定模型能力上限,推理时固定不变,与这次对话多长无关。类比:参数量=这个学生脑子的容量,窗口=这次考试桌面上允许摊开的资料页数。"
   },
   {
    "q": "多轮对话为什么第二次请求比第一次便宜/快(同一平台)?",
    "a": "前缀命中 prompt caching:前 N 轮内容完全相同,服务端复用其 KV Cache,前缀部分计费打折、延迟降低。所以 system 提示、知识库背景等稳定内容应放前面,易变的用户输入放后面。"
   },
   {
    "q": "摘要压缩会不会丢关键信息?怎么防?",
    "a": "会。摘要是又一次『有损生成』。防护:关键硬约束(数字、日期、人名、身份、过敏原)不进摘要,单独作为常驻约束行;摘要后跑一遍『要点回填校验』;重要状态改存结构化存储而不是靠摘要。"
   }
  ],
  "pitfalls": [
   "把 max_tokens 当成上下文窗口大小——max_tokens 只是输出预留。",
   "以为模型『记得』上一轮——它只记得你这次传的;记忆丢了先查应用层 messages 拼装。",
   "历史里原样回传巨大的工具 JSON,一轮吃掉半个窗口。",
   "只看标称窗口不做压力测试——超长上下文的有效利用率往往低于标称。"
  ],
  "check": {
   "q": "窗口 8K。当前 messages 合计 7.2K token,max_tokens 设 1500。会发生什么?三种处理方式?",
   "a": "输入 7.2K + 输出预留 1.5K = 8.7K > 8K,超限:要么 API 报错,要么你必须先裁剪。处理:a) 截断/压缩最旧轮次;b) 调小 max_tokens(如果输出确实不需要那么长);c) 把部分历史改为摘要或检索式注入。",
   "explain": "检验点:『输入输出共享窗口』这个约束。只盯着 max_tokens 或只盯着历史长度的人都漏了共享这一半。"
  },
  "related": [
   "PY-002",
   "LP-001",
   "RG-001",
   "FD-001"
  ],
  "doc_refs": [
   "doc-llm-1"
  ],
  "sources": [
   {
    "name": "OpenAI 官方文档 · Text Generation(消息与上下文管理)",
    "url": "https://developers.openai.com/api/docs/guides/text-generation",
    "kind": "official",
    "note": "无状态请求、窗口共享等属平台稳定行为;具体窗口大小与缓存机制版本相关,待逐项复核"
   },
   {
    "name": "独立整理",
    "url": "",
    "kind": "independent",
    "note": "lost in the middle 与大海捞针测试依据多项公开研究,表述为通用结论"
   }
  ],
  "verify": {
   "status": "partial",
   "checked_date": "2026-09-06",
   "note": "无状态与窗口机制为各平台一致的基础行为;窗口大小、prompt caching 细节随平台/版本变化,使用时查所用平台文档。"
  }
 },
 {
  "id": "LP-003",
  "topic": "llm-prompt",
  "type": "code",
  "difficulty": "intermediate",
  "title": "怎么让模型稳定输出机器可用的 JSON?提示词约束、JSON mode 和结构化输出差在哪?",
  "tags": [
   "结构化输出",
   "JSON",
   "Pydantic",
   "函数调用"
  ],
  "prerequisites": [
   "LP-001"
  ],
  "answer": "按可靠度从低到高三档:①提示词里给 schema 和示例——靠模型自觉,最弱;②JSON mode——保证输出是合法 JSON,但不保证字段符合你的预期;③结构化输出/带 strict 的函数调用——按 JSON Schema 约束解码,枚举值、必填字段都受强制。无论哪一档,服务端都必须再用 Pydantic 校验一次并准备重试,因为官方文档明确说:即使结构化输出『仍可能包含内容错误』——schema 对了,内容不一定对。",
  "plain": "让模型交作业:①口头叮嘱『按这个格式写』=提示词约束,他可能忘记;②发一张空白答题卡『随便填但必须交卡』=JSON mode,格式有了内容可能跑偏;③发一张带格子的表格,每格限定类型,写不进去就交不了=结构化输出。比喻的局限:表格保证的是『格子都在、类型合法』,不保证格子里的内容是真的——地址栏可以一本正经地填一个不存在的地址。",
  "deep": "原理与分层(OpenAI 官方文档已核验;SDK 分支语义对照本机 openai 2.34.0 源码):\n1) 提示词约束:schema 写进 prompt + few-shot 示例。任何模型可用,但遵循率取决于模型与 schema 复杂度,失败模式包括 markdown 包裹、字段改名、枚举拼错。\n2) JSON mode(格式 type: json_object):只约束『输出是合法 JSON』,不约束 schema。解决『半截 JSON/带 markdown』问题,不解决字段问题。\n3) Structured Outputs(json_schema + strict:true)或工具定义中的 strict:约束解码——生成阶段每一步只允许产出符合 schema 的 token,所以『枚举字段不会出现枚举外的值、必填字段一定在』(官方文档:确保遵循你提供的 JSON Schema)。注意官方列出的限制:部分 schema 关键字不支持;strict 要求所有对象 additionalProperties:false 且全部字段 required(本机实测:pydantic 的 model_json_schema() 不自动补 additionalProperties——要用官方 SDK 的 parse() helper 或手动补全);首次使用某 schema 有额外处理延迟。\n4) 五类失败分开处理(面试重点;SDK 2.x 行为已对照源码):\n   ① 拒答——message.refusal 非空,parse() 不抛异常、parsed 为 None:有效终态,不进格式修复重试;\n   ② 截断 / 内容过滤——parse() 直接抛 LengthFinishReasonError / ContentFilterFinishReasonError(不是返回后查 finish_reason):终态,截断的正确修法是调大输出预算;\n   ③ 临时错误——APIConnectionError / RateLimitError / 5xx:退避后原样重试(条件随时间变化);\n   ④ schema 校验失败——parse() 内部抛 pydantic ValidationError:回喂错误信息后重试(有信息增量);\n   ⑤ 业务规则失败——schema 合法但依赖外部状态的规则不满足(订单归属、库存、余额):这类规则任何 schema 都表达不了,必须应用层查询后校验,失败回喂一次或转人工。注意区分三层:schema 数值约束(exclusiveMinimum/gt=0,结构层可表达)、平台 schema 子集限制(某 API 支持哪些关键字,查官方文档)、外部业务规则(只能应用层)——不能把平台子集限制泛化成 schema 表达力的缺陷。\n5) 服务端闭环:Pydantic 定义模型 → SDK helper 生成合规 schema → 调用 → 按五类分类处理 → 仍失败降级(默认值/人工/换提示)。",
  "example": "结构化抽取的完整工程骨架(四类 API 失败分开处理 + 应用层业务校验;依赖 openai SDK 与付费 API,**本例未经真实调用**,分支语义已用 mock 测试验证——tests/lp003_mock_test.py 从本字段提取代码、桩掉 client 后逐分支断言):\n```python\nimport time\nimport openai\nfrom openai import OpenAI\nfrom typing import Literal\nfrom pydantic import BaseModel, ValidationError, Field\n\nclient = OpenAI()\nMODEL = \"gpt-4o-mini\"   # 示意;以实际接入的模型为准\nCURRENT_USER_ID = \"u_42\"   # 示意:会话里的当前登录用户\n\nclass Order(BaseModel):\n    order_id: str\n    amount: float = Field(gt=0)              # 数值约束:schema 层即可拒绝负数\n    channel: Literal[\"alipay\", \"wechat\"]     # 枚举用 Literal,strict 下受强制\n\nclass FatalExtract(Exception):\n    \"\"\"不可重试的终态:拒答 / 截断 / 内容过滤 / 重试耗尽\"\"\"\n\ndef order_belongs_to_current_user(order_id: str) -> bool:\n    \"\"\"外部状态查询(示意):真实实现查数据库。schema 管不了这类规则。\"\"\"\n    return order_id.startswith(\"u_42-\")      # 示意规则:订单号带用户前缀\n\nMAX_RETRY = 2   # 「带新信息的重试」上限;临时网络错误另按退避重试\n\ndef extract(text: str) -> Order:\n    last_err = None\n    backoff = 1\n    for attempt in range(MAX_RETRY + 1):\n        try:\n            # parse() 是官方 SDK helper:自动生成符合 strict 要求的 schema\n            # (补 additionalProperties:false 等;手写 model_json_schema() 缺这些会被 API 拒绝)\n            resp = client.chat.completions.parse(\n                model=MODEL,\n                messages=[\n                    {\"role\": \"system\", \"content\": \"从文本抽取订单字段\"},\n                    {\"role\": \"user\", \"content\": text + (f\"\\n上次错误:{last_err}\" if last_err else \"\")},\n                ],\n                response_format=Order,\n            )\n        except (openai.LengthFinishReasonError, openai.ContentFilterFinishReasonError) as e:\n            # 截断 / 内容过滤:parse() 直接抛 SDK 专属异常(不是返回后查 finish_reason)。\n            # 原样重试不携带新信息(截断要看 max_tokens 预算、内容过滤看输入),工程上按终态处理;\n            # 截断的正确修法是调大 max_tokens 预算后再调。\n            raise FatalExtract(f\"不可重试:{type(e).__name__}\") from e\n        except (openai.APIConnectionError, openai.RateLimitError, openai.InternalServerError) as e:\n            # 临时错误:退避后重试(条件随时间变化,重试有意义)\n            last_err = f\"临时错误:{type(e).__name__}\"\n            time.sleep(backoff)\n            backoff *= 2\n            continue\n        except ValidationError as e:\n            # schema 校验失败:回喂错误信息,重试有信息增量\n            last_err = str(e)\n            continue\n        msg = resp.choices[0].message\n        if getattr(msg, \"refusal\", None):\n            # 拒答是有效终态:不是校验失败,不回喂重试——回喂不改变触发条件,工程上按终态处理(转人工/换提示词)。\n            # 交人工 / 换提示词 / 改路由。\n            raise FatalExtract(f\"模型拒答:{msg.refusal}\")\n        order = msg.parsed\n        if order is None:\n            last_err = \"未返回结构化内容\"\n            continue\n        if not order_belongs_to_current_user(order.order_id):\n            # 业务规则依赖外部状态(订单归属):schema/数值约束都表达不了,\n            # 必须应用层查询校验。失败回喂一次,仍失败终态。\n            last_err = f\"订单 {order.order_id} 不属于当前用户\"\n            continue\n        return order\n    raise FatalExtract(f\"抽取失败(重试 {MAX_RETRY} 次后):{last_err}\")\n```\n三层校验的分工(本机 pydantic 2.10.4 运行验证):①schema 只写 `amount: float` 时,-3 合法通过(类型对就行);②`Field(gt=0)` 后结构校验层拒绝,schema 产物含 `exclusiveMinimum: 0`——数值约束是 schema 表达力的一部分;③『这笔订单是不是当前用户的』依赖数据库/会话状态,任何 schema 都表达不了,必须应用层查询。",
  "interview": "口述版:『我会分三档说:提示词约束最弱;JSON mode 保证合法 JSON 不保证 schema;结构化输出用 JSON Schema 约束解码,strict 模式下枚举和必填都是强制的。但我会强调官方文档自己都写了「结构化输出仍可能有内容错误」,所以我的服务端一定有 Pydantic 校验加带错误回喂的重试闭环,三次失败就降级。schema 设计上尽量扁平,别用平台不支持的关键字。』追问展开:schema 太深的后果、refusal 处理、不支持 strict 的模型怎么办。",
  "followups": [
   {
    "q": "schema 很复杂、嵌套很深会怎样?",
    "a": "约束解码的搜索空间变大:首个请求延迟更高,生成更慢,极端情况质量下降(模型被迫在受限候选里选择)。实践:schema 扁平化、用扁平枚举代替深层 if-else 结构、把一个大抽取拆成两次简单调用。"
   },
   {
    "q": "模型在结构化输出里『拒答』怎么处理?",
    "a": "OpenAI 用独立的 refusal 字段承载拒答, content 可能为空——解析时要先查 refusal 再 validate。业务上把拒答当成有效结果(转人工/换话术),不要当成异常无限重试。"
   },
   {
    "q": "用的模型不支持 strict 结构化输出怎么办?",
    "a": "降级组合拳:JSON mode + 明确 schema 的提示词 + 2~3 个 few-shot 输出示例 + Pydantic 宽松解析(alias、默认值)+ 带错误回喂的重试。同时低温(0~0.3)显著提高遵循率。"
   }
  ],
  "pitfalls": [
   "以为 JSON mode 能保证字段正确——它只保证『是 JSON』。",
   "把 model_json_schema() 的产物直接丢给 strict:true——缺少 additionalProperties:false 会被 API 拒绝(本机 pydantic 2.10.4 实测不自动带),要用 SDK 的 parse() helper 或手动补全。",
   "把拒答当校验失败回喂重试——回喂不改变触发条件,工程上按终态处理,应转人工或换提示(本例 mock 实测:拒答 1 次调用即终止)。",
   "以为 parse() 截断时能靠返回值里的 finish_reason 判断——SDK 2.x 直接抛 LengthFinishReasonError / ContentFilterFinishReasonError,要在异常分支处理。",
   "把三层校验混为一谈——①数值约束(金额为正)schema 就能表达(exclusiveMinimum/Pydantic gt=0,本机实测);②某平台不支持某关键字是平台子集限制,不是 schema 表达力的缺陷;③订单归属/库存/余额这类依赖外部状态的规则才必须应用层校验。三层混谈会得出『schema 表达不了业务规则』的笼统错误结论。"
  ],
  "check": {
   "q": "三个判断:①Order(amount=-3) 在『amount: float』的 schema 下能通过吗?怎么让它在结构校验层就被拒绝?②你所用平台的结构化输出不支持 exclusiveMinimum,这是 schema 表达力的缺陷吗?③『这笔订单是不是当前用户下的』为什么 schema 管不了?",
   "a": "①能通过——float 只约束类型,-3 是合法浮点数(本机实测);给字段加数值约束(gField(gt=0),schema 产物即 exclusiveMinimum: 0)后结构校验层直接拒绝。②不是。那是该平台支持的 JSON Schema 子集限制——JSON Schema 本身有数值约束关键字;换支持的版本/平台,或在应用层补数值检查,并按平台文档确认子集。③它依赖外部状态(订单归属存在数据库里):schema 是纯结构声明,管不了任何需要查询外部数据才能回答的问题;必须应用层查询后校验,失败按业务规则处理(回喂/转人工),而不是当作格式错误无限重试。",
   "explain": "检验点:三层边界——schema 能表达什么(exclusiveMinimum)、平台子集限制(查文档)、外部状态规则(应用层);以及『原样重试』只是无信息增量的失败才不该做,不是绝对定律。"
  },
  "related": [
   "LP-001",
   "LP-004",
   "AG-002"
  ],
  "doc_refs": [
   "doc-llm-1"
  ],
  "sources": [
   {
    "name": "OpenAI 官方文档 · Structured Outputs",
    "url": "https://developers.openai.com/api/docs/guides/structured-outputs",
    "kind": "official",
    "note": "2026-09-06 已核验:json_schema/strict 语义、与 JSON mode 对比、refusal、限制条款"
   }
  ],
  "verify": {
   "status": "verified",
   "checked_date": "2026-09-07",
   "note": "三档区别、strict 语义与限制、refusal/截断语义已对 OpenAI 官方文档核验;『model_json_schema 不带 additionalProperties』已在本机 pydantic 2.10.4 运行验证;『float 放行 -3 / gt=0 拒绝且产物含 exclusiveMinimum』已本机运行验证;SDK 异常分支对照本机 openai 2.34.0 源码;展示代码分支由 tests/lp003_mock_test.py 桩客户端逐分支验证(mock,非真实 API 调用);平台子集限制以各平台官方文档为准。"
  },
  "content_version": {
   "rev": "2026-09-07",
   "summary": "结构化输出代码按五类失败重写:拒答是终态不重试、截断走 SDK 异常、临时错误退避重试、业务校验与类型校验分离。",
   "changes": [
    "例子代码全部分支经 mock 测试验证",
    "strict 需 additionalProperties 的说明与 SDK helper 用法"
   ]
  }
 },
 {
  "id": "LP-004",
  "topic": "llm-prompt",
  "type": "scenario",
  "difficulty": "intermediate",
  "title": "线上调用 LLM API 突然大量 429,你的错误处理和重试策略该怎么设计?",
  "tags": [
   "错误处理",
   "重试",
   "限流",
   "指数退避",
   "可靠性"
  ],
  "prerequisites": [
   "PY-003"
  ],
  "answer": "先把错误分类,再决定动作:429(限流)→ 指数退避加随机抖动地重试,优先遵守响应头 Retry-After;超时和 5xx → 少量重试且必须设超时;400/401/403 → 程序错误,重试无意义,应该告警修代码。重试必须有次数上限并保证幂等,高并发下还要在客户端做并发限制/排队削峰,重试风暴本身就会制造新的 429。",
  "plain": "像打客服热线:听到『坐席忙』(429)就等一会儿再打,而且越忙等得越久——这就是指数退避;『您拨打的号码不存在』(400)重拨一万次也没用,得改号码。『抖动』是防『全公司所有人都在整点同一秒重拨』——如果大家同时重试,永远占线,所以要各自加一点随机延迟错开。比喻的局限:真实系统里你还要问『为什么占线』——是不是自己并发太高触发了配额,那治本要靠限流排队,而不是一直礼貌地重拨。",
  "deep": "逐类拆解:\n1) 429 Too Many Requests:触发方可能是 RPM/TPM 配额或突发。处理顺序:读 Retry-After(有就照办)→ 指数退避 delay = min(cap, base * 2^attempt) + uniform(0, jitter);attempt 上限 3~5;同时全局并发用信号量/令牌桶控制,把 429 率压下来。SDK(OpenAI/Azure 等)通常内置自动重试,自研层要做的是并发控制与降级。\n2) 超时:连接超时、首字节超时、总时长超时要分开设;流式接口中途断开属于部分成功,重试会重算整个回答(成本翻倍),前端要提示。幂等:生成类请求重试会产生重复计费/重复消息,业务层用请求 ID 去重。\n3) 5xx/网络错误:可重试,同样的退避;连续失败达到阈值触发熔断——一段时间内直接快速失败,避免雪崩,并降级到备用模型/缓存/兜底文案。\n4) 4xx(400 参数错、401 认证、403 权限、422):不重试,立即告警并记录完整请求(脱敏),这是代码或配置 bug。\n5) 降级链设计:主模型→备用模型(提示词需适配)→缓存答案→固定话术。每一级都要记录降级原因用于事后统计。\n6) 可观测性:429 率、重试率、熔断次数、P50/P95/P99 延迟、每次成本——这些指标是容量规划和配额申请的依据。",
  "example": "```python\nimport random, time, httpx\n\ndef call_llm(payload, max_retry=4):\n    for attempt in range(max_retry):\n        try:\n            r = httpx.post(URL, json=payload, timeout=(5, 60))\n            if r.status_code == 429:\n                wait = float(r.headers.get(\"retry-after\", 0))\n                wait = max(wait, min(30, 2 ** attempt) + random.uniform(0, 1))\n                time.sleep(wait)          # 优先 Retry-After,退避+抖动兜底\n                continue\n            if r.status_code >= 500:\n                time.sleep(min(10, 2 ** attempt) + random.random())\n                continue\n            if r.status_code >= 400:      # 400/401/403:重试无意义\n                raise RuntimeError(f\"非瞬时错误 {r.status_code}: {r.text[:200]}\")\n            return r.json()\n        except httpx.TimeoutException:\n            if attempt == max_retry - 1:\n                raise\n            time.sleep(min(10, 2 ** attempt))\n    raise RuntimeError(\"重试次数耗尽\")      # 上层接降级:备用模型/缓存\n```",
  "interview": "口述版:『我会按「能不能靠重试解决」给错误分类:429、超时、5xx 是瞬时问题,退避重试但要抖动、设上限、守 Retry-After;4xx 是程序问题,重试只会烧钱,要告警。系统层面我会加并发控制把 429 压在源头,配熔断和降级链——主模型挂了走备用模型或缓存。重试请求我会做幂等去重,防止重复计费。』追问展开:流式中途断开怎么处理、多模型降级设计、429 率监控与容量规划。",
  "followups": [
   {
    "q": "流式响应生成到一半断了,怎么办?",
    "a": "先区分:已发出的部分对用户可见。策略:a) 前端展示已收内容并给『继续生成』按钮,后端按上下文续写;b) 整段重试但要计费去重;c) 有平台支持流式重连/续传(版本相关)。关键是别把半截回答当完整回答入库。"
   },
   {
    "q": "怎么做多模型降级?",
    "a": "抽象统一网关层:模型名、参数、返回格式适配;健康探测与熔断;按『主→备→缓存→兜底话术』顺序切换,切换要带原因标记。注意提示词在不同模型间表现不同,降级模型要有自己的提示词版本和评估,不是换个名字就行。"
   },
   {
    "q": "为什么重试要加『抖动』?",
    "a": "没有抖动时,所有超时请求会在同一时刻重试,形成同步重试风暴,把瞬时故障变成持续拒绝服务。加随机延迟把重试在时间上摊开,这是分布式系统的标准做法(thundering herd 问题)。"
   }
  ],
  "pitfalls": [
   "对 400/401 也无脑重试,白白烧钱还掩盖 bug。",
   "重试没有上限或没有总超时,用户端等到天荒地老。",
   "忽略 Retry-After,用自己拍脑袋的间隔,反而更容易再次 429。",
   "只在单请求层面处理,没有全局并发控制——429 是配额问题,单请求重试救不了全局。"
  ],
  "check": {
   "q": "你收到 429,响应头带 Retry-After: 30。你的退避公式算出 5 秒。应该等几秒?",
   "a": "以 Retry-After 为准,等约 30 秒(可以再加一点抖动)。服务器明确告诉你何时再来,照办是协议礼仪,也能显著降低再次被拒的概率。",
   "explain": "检验点:优先级——显式服务器指令 > 本地退避公式。答 5 秒说明没理解 Retry-After 的地位。"
  },
  "related": [
   "PY-003",
   "LP-003",
   "EN-001",
   "EN-002"
  ],
  "doc_refs": [
   "doc-llm-1"
  ],
  "sources": [
   {
    "name": "OpenAI 官方文档 · 错误码与速率限制",
    "url": "https://developers.openai.com/api/docs/error-codes",
    "kind": "official",
    "note": "错误码语义本次未逐页抓取,属平台公开稳定信息;退避与抖动为业界通行做法"
   },
   {
    "name": "独立整理",
    "url": "",
    "kind": "independent",
    "note": "指数退避/抖动/熔断依据分布式系统通行实践独立撰写"
   }
  ],
  "verify": {
   "status": "partial",
   "checked_date": "2026-09-06",
   "note": "错误分类与退避策略为稳定工程实践;各家 SDK 重试默认值、配额维度随版本变化,面试时建议声明以所用平台为准。代码示例未运行,仅为模式演示。"
  }
 }
]
